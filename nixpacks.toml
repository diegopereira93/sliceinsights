# Nixpacks configuration for Railway
# Using providers for automatic Python detection

[phases.setup]
nixPkgs = ["postgresql"]

# Let Nixpacks auto-detect Python from requirements.txt
# and use its built-in pip installation

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} --workers 4"
