# ============================================
# REQUIRED: Database Configuration
# ============================================
# ⚠️ IMPORTANT: Change these values in production!
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres  # Change in production!
POSTGRES_DB=picklematch
DB_HOST=postgres_v3
DB_PORT=5432

# ============================================
# Backend Configuration
# ============================================
DEBUG=false  # Set to false in production
LOG_LEVEL=INFO
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DB_HOST}:${DB_PORT}/${POSTGRES_DB}
DATABASE_URL_SYNC=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DB_HOST}:${DB_PORT}/${POSTGRES_DB}

# CORS - Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8002

# ============================================
# Frontend Configuration
# ============================================
NEXT_PUBLIC_API_URL=http://localhost:8002/api/v1

# ============================================
# Observability (Optional)
# ============================================
SENTRY_DSN=  # Add your Sentry DSN for error tracking
