---
phase: 17-ui-redesign-com-stitch
plan: 04
type: execute
wave: 3
depends_on: [17-02, 17-03]
files_modified:
  - frontend/e2e/responsiveness.spec.ts
autonomous: false
requirements: [UI-04, UI-05]

must_haves:
  truths:
    - "All 3 pages render without horizontal overflow at 375px, 768px, 1280px"
    - "All existing E2E tests still pass (26/26 baseline)"
    - "Human has visually verified the redesigned pages look correct"
  artifacts:
    - path: "frontend/e2e/responsiveness.spec.ts"
      provides: "Complete responsive viewport tests"
      contains: "scrollWidth"
  key_links:
    - from: "frontend/e2e/responsiveness.spec.ts"
      to: "frontend/playwright.config.ts"
      via: "projects config defines viewports"
      pattern: "mobile.*tablet.*desktop"
---

<objective>
Run the complete E2E regression suite across all viewports, verify responsive behavior at 375px/768px/1280px for all redesigned pages, and get human visual approval of the redesign quality.

Purpose: This is the quality gate for Phase 17. All visual changes from Plans 02 and 03 must be validated both automatically (E2E tests, viewport overflow checks) and manually (human visual inspection).

Output: Green E2E suite, passing responsive tests, and human approval of visual quality.
</objective>

<execution_context>
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/workflows/execute-plan.md
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ui-redesign-com-stitch-implementar-designs-stitch-ai-para-elite-racket-catalog-premium-home-quiz-t-cnico-e-analytics-com-responsividade-total/17-02-SUMMARY.md
@.planning/phases/17-ui-redesign-com-stitch-implementar-designs-stitch-ai-para-elite-racket-catalog-premium-home-quiz-t-cnico-e-analytics-com-responsividade-total/17-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full E2E regression and responsive viewport tests</name>
  <files>frontend/e2e/responsiveness.spec.ts</files>
  <read_first>
    - frontend/e2e/responsiveness.spec.ts (scaffold from Plan 01 — may need enhancement)
    - frontend/e2e/sliceinsights.spec.ts (main test suite — verify selectors still match)
    - frontend/e2e/recommendation-e2e.spec.ts (recommendation tests)
    - frontend/playwright.config.ts (verify projects config from Plan 01)
  </read_first>
  <action>
1. **Enhance responsiveness.spec.ts** if needed — ensure it tests all 3 routes at all 3 viewports. The scaffold from Plan 01 should already have the basic structure. Add these specific checks if not present:

   ```typescript
   // For /recommend specifically — verify step buttons are tappable at 375px
   test(`/recommend buttons are accessible at mobile viewport`, async ({ page }) => {
     await page.setViewportSize({ width: 375, height: 812 });
     await page.goto('/recommend');
     await page.waitForLoadState('networkidle');
     // Verify at least one button is visible and clickable
     const buttons = page.getByRole('button');
     await expect(buttons.first()).toBeVisible({ timeout: 15000 });
   });

   // For / specifically — verify paddle cards render
   test(`/ displays cards at mobile viewport`, async ({ page }) => {
     await page.setViewportSize({ width: 375, height: 812 });
     await page.goto('/');
     await page.waitForLoadState('networkidle');
     const cards = page.locator('[class*="card"], [class*="grid"] > div');
     await expect(cards.first()).toBeVisible({ timeout: 15000 });
   });
   ```

2. **Run full regression suite on desktop project**:
   ```bash
   cd /home/diego/Documentos/projetos/data-products/sliceinsights/frontend
   BASE_URL=http://localhost:3002 API_URL=http://localhost:8002/api/v1 npx playwright test --project=desktop --reporter=list
   ```
   All non-skipped tests must pass.

3. **Run responsive tests on mobile project**:
   ```bash
   BASE_URL=http://localhost:3002 API_URL=http://localhost:8002/api/v1 npx playwright test e2e/responsiveness.spec.ts --project=mobile --reporter=list
   ```

4. **Run responsive tests on tablet project**:
   ```bash
   BASE_URL=http://localhost:3002 API_URL=http://localhost:8002/api/v1 npx playwright test e2e/responsiveness.spec.ts --project=tablet --reporter=list
   ```

5. If any test fails:
   - For overflow issues: check the specific page for elements with `position: fixed` or `width: 100vw` without overflow-hidden
   - For selector issues: check if redesign changed text content or semantic HTML roles
   - Fix the source component (not the test) unless the test selector was fragile
   - Re-run after fix

6. Document final results: total passing, total failing, total skipped.
  </action>
  <verify>
    <automated>cd /home/diego/Documentos/projetos/data-products/sliceinsights/frontend && BASE_URL=http://localhost:3002 API_URL=http://localhost:8002/api/v1 npx playwright test --project=desktop --reporter=list 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --project=desktop` exits with 0 failures on non-skipped tests
    - `npx playwright test e2e/responsiveness.spec.ts --project=mobile` exits with 0 failures
    - `npx playwright test e2e/responsiveness.spec.ts --project=tablet` exits with 0 failures
    - responsiveness.spec.ts tests at least 3 routes
  </acceptance_criteria>
  <done>Full E2E suite passes on desktop. Responsive tests pass on mobile and tablet. No regressions from redesign.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of redesigned pages</name>
  <files></files>
  <action>
Present the redesigned pages to the user for visual verification. All automated tests have already passed in Task 1. This checkpoint confirms that the visual quality meets the user's expectations for premium design inspired by Stitch AI.

Pages to verify:
1. Home (http://localhost:3002/) — enhanced hero, PaddleCard hover glow
2. Quiz (http://localhost:3002/recommend) — design system tokens, framer-motion animations
3. Statistics (http://localhost:3002/statistics) — refined tabs and card containers

Verification checklist provided in how-to-verify section below.
  </action>
  <verify>Human visual approval — user types "approved" or describes issues</verify>
  <done>User has approved the visual quality of all 3 redesigned pages at desktop and mobile viewports.</done>
  <what-built>
    Complete UI redesign of 3 pages:
    1. `/recommend` — Migrated from hardcoded hex to design system tokens, added framer-motion animations, SSR wrapper
    2. `/` (Home) — Enhanced hero section with gradient, PaddleCard hover glow effects
    3. `/statistics` — Refined tab styling, improved card containers and data presentation
  </what-built>
  <how-to-verify>
    Open the app at http://localhost:3002 and check each page:

    **1. Home page (http://localhost:3002/)**
    - [ ] Hero section has premium look with subtle gradient
    - [ ] Paddle cards have hover glow effect (hover over a card)
    - [ ] Cards lift slightly on hover
    - [ ] Filters still work (open filter drawer, apply a filter)
    - [ ] Check at 375px width (mobile responsive — use browser dev tools)

    **2. Quiz page (http://localhost:3002/recommend)**
    - [ ] No visual "flash" of unstyled content (lime-green on black should look premium)
    - [ ] Step transitions animate smoothly (sliding left/right)
    - [ ] Option buttons are pill-shaped with proper hover states
    - [ ] Progress bar shows current step position
    - [ ] Complete the quiz — results display in Card components
    - [ ] Chat panel works and shows messages
    - [ ] Check at 375px width (mobile)

    **3. Statistics page (http://localhost:3002/statistics)**
    - [ ] Tabs have refined styling (active tab highlighted)
    - [ ] Charts render correctly (Recharts not broken)
    - [ ] Data cards have consistent styling
    - [ ] Check at 375px width (mobile)

    **Cross-cutting:**
    - [ ] Bottom navigation works on all pages
    - [ ] No broken images or missing icons
    - [ ] No console errors (open browser dev tools -> Console)
  </how-to-verify>
  <resume-signal>Type "approved" if the redesign looks good, or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Full E2E suite passes on desktop project
2. Responsive tests pass on mobile and tablet projects
3. Human approves visual quality of all 3 redesigned pages
</verification>

<success_criteria>
- All E2E tests pass (desktop, mobile, tablet)
- No horizontal overflow on any page at any viewport
- Human confirms visual quality is premium and consistent
- Phase 17 is complete
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-redesign-com-stitch-implementar-designs-stitch-ai-para-elite-racket-catalog-premium-home-quiz-t-cnico-e-analytics-com-responsividade-total/17-04-SUMMARY.md`
</output>
