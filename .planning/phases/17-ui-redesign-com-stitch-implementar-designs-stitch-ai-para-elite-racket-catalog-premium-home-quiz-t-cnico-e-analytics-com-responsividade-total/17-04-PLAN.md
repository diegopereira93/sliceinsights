---
phase: 17-ui-redesign
plan: 04
type: execute
wave: 3
depends_on: [17-03]
files_modified:
  - frontend-vite/playwright.config.ts
  - frontend-vite/e2e/pages.spec.ts
  - frontend-vite/e2e/api-compat.spec.ts
  - frontend-vite/e2e/responsiveness.spec.ts
  - frontend-vite/package.json
autonomous: true
requirements: [UI-05]

must_haves:
  truths:
    - "Playwright config exists with 3 viewport projects (desktop, mobile, tablet)"
    - "E2E test verifies all 4 routes load without error"
    - "E2E test verifies API calls to FastAPI return 200"
    - "E2E test verifies no horizontal overflow at 375px, 768px, and 1280px viewports"
    - "All E2E tests pass"
  artifacts:
    - path: "frontend-vite/playwright.config.ts"
      provides: "Playwright config with viewport projects"
      contains: "baseURL"
    - path: "frontend-vite/e2e/pages.spec.ts"
      provides: "Page loading tests for all 4 routes"
      contains: "/recommend"
    - path: "frontend-vite/e2e/api-compat.spec.ts"
      provides: "API compatibility tests"
      contains: "/api/paddles"
    - path: "frontend-vite/e2e/responsiveness.spec.ts"
      provides: "Responsive layout tests"
      contains: "375"
  key_links:
    - from: "frontend-vite/playwright.config.ts"
      to: "frontend-vite/e2e/*.spec.ts"
      via: "testDir: './e2e'"
      pattern: "testDir"
---

<objective>
Create Playwright E2E test suite for the new Vite SPA frontend: config with 3 viewport projects, page loading tests for all 4 routes, API compatibility tests, and responsiveness tests.

Purpose: Automated validation that the migration is complete and all pages work correctly with real FastAPI data.

Output: Green E2E test suite in `frontend-vite/e2e/` with Playwright config.
</objective>

<execution_context>
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/workflows/execute-plan.md
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ui-redesign-com-stitch-implementar-designs-stitch-ai-para-elite-racket-catalog-premium-home-quiz-t-cnico-e-analytics-com-responsividade-total/17-CONTEXT.md
@.planning/phases/17-ui-redesign-com-stitch-implementar-designs-stitch-ai-para-elite-racket-catalog-premium-home-quiz-t-cnico-e-analytics-com-responsividade-total/17-VALIDATION.md
@.planning/phases/17-ui-redesign-com-stitch-implementar-designs-stitch-ai-para-elite-racket-catalog-premium-home-quiz-t-cnico-e-analytics-com-responsividade-total/17-UI-SPEC.md
@.planning/phases/17-ui-redesign-com-stitch-implementar-designs-stitch-ai-para-elite-racket-catalog-premium-home-quiz-t-cnico-e-analytics-com-responsividade-total/17-01-SUMMARY.md
@.planning/phases/17-ui-redesign-com-stitch-implementar-designs-stitch-ai-para-elite-racket-catalog-premium-home-quiz-t-cnico-e-analytics-com-responsividade-total/17-02-SUMMARY.md
@.planning/phases/17-ui-redesign-com-stitch-implementar-designs-stitch-ai-para-elite-racket-catalog-premium-home-quiz-t-cnico-e-analytics-com-responsividade-total/17-03-SUMMARY.md

<interfaces>
<!-- Source: frontend/playwright.config.ts (existing config to adapt) -->
<!-- Existing patterns from earlier phases -->
Viewport projects: devices['iPhone 13'] (mobile), devices['iPad (gen 7)'] (tablet), 'Desktop Chrome' (desktop)
baseURL: process.env.BASE_URL || 'http://localhost:3002'

<!-- Source: 17-VALIDATION.md -->
Test files needed:
- frontend-vite/e2e/pages.spec.ts — 4 routes load without error
- frontend-vite/e2e/api-compat.spec.ts — API calls return 200
- frontend-vite/e2e/responsiveness.spec.ts — no overflow at mobile/tablet/desktop

<!-- Source: 17-UI-SPEC.md — route mapping -->
/ -> Home (paddle cards, hero, filters)
/recommend -> Quiz (multi-step wizard)
/statistics -> Stats (KPI cards, charts)
/chat -> Chat (AI coach interface)

<!-- Source: 17-UI-SPEC.md — BottomNav -->
aria-label: "Inicio" (Home), "Recomendacao" (AI Coach), "Analise" (Statistics)

<!-- Source: 17-UI-SPEC.md — copywriting contract -->
Loading state catalog: "Carregando arsenal..."
Stats page title: "RAIO-X DO MERCADO"
Chat greeting: "Bem-vindo ao Command Center"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright and create config with viewport projects</name>
  <files>
    frontend-vite/playwright.config.ts
    frontend-vite/package.json
  </files>
  <read_first>
    frontend/playwright.config.ts
    frontend/package.json
    frontend-vite/package.json
  </read_first>
  <action>
    **Step 1: Install Playwright in frontend-vite**
    ```bash
    cd frontend-vite && npm install --save-dev @playwright/test && npx playwright install chromium
    ```

    **Step 2: Add E2E scripts to package.json**
    Add to `frontend-vite/package.json` scripts:
    ```json
    "test:e2e": "playwright test",
    "test:e2e:desktop": "playwright test --project=desktop --reporter=dot",
    "test:e2e:full": "playwright test --project=desktop,mobile,tablet"
    ```

    **Step 3: Create playwright.config.ts**
    Adapt from existing `frontend/playwright.config.ts` with these changes:
    - `baseURL`: `process.env.BASE_URL || 'http://localhost:3002'` (port 3002 for Vite)
    - `testDir`: `'./e2e'`
    - Three projects:
      - `desktop`: viewport `{ width: 1280, height: 720 }`
      - `mobile`: use `devices['iPhone 13']`
      - `tablet`: use `devices['iPad (gen 7)']`
    - `webServer` config (optional): start Vite dev server if not running:
      ```typescript
      webServer: {
        command: 'npm run dev',
        port: 3002,
        reuseExistingServer: true,
        timeout: 30000,
      },
      ```
    - `use.trace`: `'on-first-retry'`
    - `retries`: 1
    - `timeout`: 30000

    Full config:
    ```typescript
    import { defineConfig, devices } from '@playwright/test';

    export default defineConfig({
      testDir: './e2e',
      fullyParallel: true,
      forbidOnly: !!process.env.CI,
      retries: process.env.CI ? 2 : 1,
      workers: process.env.CI ? 1 : undefined,
      reporter: 'html',
      timeout: 30000,
      use: {
        baseURL: process.env.BASE_URL || 'http://localhost:3002',
        trace: 'on-first-retry',
      },
      projects: [
        {
          name: 'desktop',
          use: { ...devices['Desktop Chrome'] },
        },
        {
          name: 'mobile',
          use: { ...devices['iPhone 13'] },
        },
        {
          name: 'tablet',
          use: { ...devices['iPad (gen 7)'] },
        },
      ],
      webServer: {
        command: 'npm run dev',
        port: 3002,
        reuseExistingServer: true,
        timeout: 30000,
      },
    });
    ```
  </action>
  <verify>
    <automated>cd /home/diego/Documentos/projetos/data-products/sliceinsights/frontend-vite && grep "desktop" playwright.config.ts && grep "mobile" playwright.config.ts && grep "tablet" playwright.config.ts && grep "3002" playwright.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend-vite/playwright.config.ts` exists
    - Config contains `testDir: './e2e'`
    - Config contains 3 projects named `desktop`, `mobile`, `tablet`
    - Config `baseURL` contains `3002`
    - `frontend-vite/package.json` contains `test:e2e` script
    - `@playwright/test` is in devDependencies
  </acceptance_criteria>
  <done>Playwright installed, config created with 3 viewport projects, E2E scripts added to package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Create E2E test files — pages, API compat, and responsiveness</name>
  <files>
    frontend-vite/e2e/pages.spec.ts
    frontend-vite/e2e/api-compat.spec.ts
    frontend-vite/e2e/responsiveness.spec.ts
  </files>
  <read_first>
    frontend/e2e/sliceinsights.spec.ts
    frontend/e2e/recommendation-e2e.spec.ts
    frontend/e2e/responsiveness.spec.ts
    frontend-vite/playwright.config.ts
    frontend-vite/src/App.tsx
    frontend-vite/src/components/BottomNav.tsx
  </read_first>
  <action>
    **File 1: frontend-vite/e2e/pages.spec.ts**
    Tests that all 4 routes load successfully without JavaScript errors.

    ```typescript
    import { test, expect } from '@playwright/test';

    test.describe('Page Loading', () => {
      test('Home page (/) loads and displays paddle cards', async ({ page }) => {
        await page.goto('/');
        // Wait for content to load (not just skeleton)
        await expect(page.locator('body')).toBeVisible();
        // Check title or hero content exists
        await expect(page.locator('text=DOMINE A QUADRA')).toBeVisible({ timeout: 10000 });
        // Check BottomNav is present
        await expect(page.locator('nav')).toBeVisible();
      });

      test('Recommend page (/recommend) loads quiz', async ({ page }) => {
        await page.goto('/recommend');
        await expect(page.locator('body')).toBeVisible();
        // Quiz should have step content or selection options
        // Wait for page to be interactive
        await page.waitForLoadState('networkidle');
      });

      test('Statistics page (/statistics) loads', async ({ page }) => {
        await page.goto('/statistics');
        await expect(page.locator('body')).toBeVisible();
        // Stats page shows title
        await expect(page.locator('text=RAIO-X DO MERCADO').or(page.locator('text=CARREGANDO TERMINAL'))).toBeVisible({ timeout: 10000 });
      });

      test('Chat page (/chat) loads', async ({ page }) => {
        await page.goto('/chat');
        await expect(page.locator('body')).toBeVisible();
        // Chat should show greeting or input
        await page.waitForLoadState('networkidle');
      });

      test('BottomNav navigates between pages', async ({ page }) => {
        await page.goto('/');
        // Click AI Coach nav item
        const recommendLink = page.locator('a[href="/recommend"], [aria-label*="Recomenda"]').first();
        if (await recommendLink.isVisible()) {
          await recommendLink.click();
          await expect(page).toHaveURL(/\/recommend/);
        }
      });

      test('404 page for unknown routes', async ({ page }) => {
        await page.goto('/unknown-route-xyz');
        await expect(page.locator('body')).toBeVisible();
        // Should show not-found content (not crash)
      });

      test('No JavaScript console errors on Home', async ({ page }) => {
        const errors: string[] = [];
        page.on('console', msg => {
          if (msg.type() === 'error') errors.push(msg.text());
        });
        await page.goto('/');
        await page.waitForLoadState('networkidle');
        // Filter out expected errors (e.g., favicon 404)
        const realErrors = errors.filter(e => !e.includes('favicon'));
        expect(realErrors).toHaveLength(0);
      });
    });
    ```

    **File 2: frontend-vite/e2e/api-compat.spec.ts**
    Tests that the FastAPI shim endpoints respond correctly.

    ```typescript
    import { test, expect } from '@playwright/test';

    const API_URL = process.env.API_URL || 'http://localhost:8002';

    test.describe('API Compatibility', () => {
      test('GET /api/healthz returns status ok', async ({ request }) => {
        const response = await request.get(`${API_URL}/api/healthz`);
        expect(response.status()).toBe(200);
        const body = await response.json();
        expect(body.status).toBe('ok');
      });

      test('GET /api/paddles returns PaddleListResponse', async ({ request }) => {
        const response = await request.get(`${API_URL}/api/paddles`);
        expect(response.status()).toBe(200);
        const body = await response.json();
        expect(body).toHaveProperty('paddles');
        expect(body).toHaveProperty('total');
        expect(Array.isArray(body.paddles)).toBe(true);
        if (body.paddles.length > 0) {
          const paddle = body.paddles[0];
          expect(paddle).toHaveProperty('id');
          expect(paddle).toHaveProperty('name');
          expect(paddle).toHaveProperty('brand');
          expect(paddle).toHaveProperty('powerScore');
          expect(paddle).toHaveProperty('controlScore');
          expect(typeof paddle.id).toBe('number');
        }
      });

      test('GET /api/paddles supports query params', async ({ request }) => {
        const response = await request.get(`${API_URL}/api/paddles?limit=5&offset=0`);
        expect(response.status()).toBe(200);
        const body = await response.json();
        expect(body.paddles.length).toBeLessThanOrEqual(5);
      });

      test('GET /api/stats/market returns MarketStats', async ({ request }) => {
        const response = await request.get(`${API_URL}/api/stats/market`);
        expect(response.status()).toBe(200);
        const body = await response.json();
        expect(body).toHaveProperty('totalPaddles');
        expect(body).toHaveProperty('averagePrice');
        expect(body).toHaveProperty('coreThicknessDistribution');
        expect(body).toHaveProperty('priceRangeDistribution');
        expect(body).toHaveProperty('powerVsControlData');
        expect(typeof body.totalPaddles).toBe('number');
      });

      test('GET /api/stats/brands returns BrandStat array', async ({ request }) => {
        const response = await request.get(`${API_URL}/api/stats/brands`);
        expect(response.status()).toBe(200);
        const body = await response.json();
        expect(Array.isArray(body)).toBe(true);
        if (body.length > 0) {
          expect(body[0]).toHaveProperty('brand');
          expect(body[0]).toHaveProperty('count');
          expect(body[0]).toHaveProperty('avgPrice');
          expect(body[0]).toHaveProperty('marketShare');
        }
      });

      test('GET /api/stats/hidden-gems returns Paddle array', async ({ request }) => {
        const response = await request.get(`${API_URL}/api/stats/hidden-gems`);
        expect(response.status()).toBe(200);
        const body = await response.json();
        expect(Array.isArray(body)).toBe(true);
      });

      test('POST /api/leads accepts LeadInput', async ({ request }) => {
        const response = await request.post(`${API_URL}/api/leads`, {
          data: {
            name: 'E2E Test User',
            email: `e2e-test-${Date.now()}@example.com`,
          },
        });
        expect(response.status()).toBe(200);
        const body = await response.json();
        expect(body).toHaveProperty('success');
        expect(body.success).toBe(true);
      });
    });
    ```

    **File 3: frontend-vite/e2e/responsiveness.spec.ts**
    Tests that no horizontal overflow occurs at all viewport sizes.

    ```typescript
    import { test, expect } from '@playwright/test';

    const routes = ['/', '/recommend', '/statistics', '/chat'];

    for (const route of routes) {
      test.describe(`Responsiveness: ${route}`, () => {
        test(`no horizontal overflow at any viewport`, async ({ page }) => {
          await page.goto(route);
          await page.waitForLoadState('networkidle');

          // Check that document width equals viewport width (no overflow)
          const hasOverflow = await page.evaluate(() => {
            return document.documentElement.scrollWidth > document.documentElement.clientWidth;
          });
          expect(hasOverflow).toBe(false);
        });
      });
    }
    ```

    Create `frontend-vite/e2e/` directory first:
    ```bash
    mkdir -p frontend-vite/e2e
    ```
  </action>
  <verify>
    <automated>cd /home/diego/Documentos/projetos/data-products/sliceinsights/frontend-vite && ls e2e/*.spec.ts | wc -l && npx playwright test --project=desktop --reporter=dot 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `frontend-vite/e2e/pages.spec.ts` exists and contains tests for `/`, `/recommend`, `/statistics`, `/chat`
    - `frontend-vite/e2e/api-compat.spec.ts` exists and contains tests for `/api/healthz`, `/api/paddles`, `/api/stats/market`, `/api/stats/brands`, `/api/stats/hidden-gems`, `/api/leads`
    - `frontend-vite/e2e/responsiveness.spec.ts` exists and contains overflow check
    - `grep "/recommend" frontend-vite/e2e/pages.spec.ts` matches
    - `grep "/statistics" frontend-vite/e2e/pages.spec.ts` matches
    - `grep "/api/paddles" frontend-vite/e2e/api-compat.spec.ts` matches
    - `grep "scrollWidth" frontend-vite/e2e/responsiveness.spec.ts` matches
    - `ls frontend-vite/e2e/*.spec.ts | wc -l` returns 3
  </acceptance_criteria>
  <done>Three E2E test files created covering page loading (4 routes), API contract compatibility (6 endpoints), and responsive layout (no overflow at all viewports). Tests pass against running stack.</done>
</task>

</tasks>

<verification>
1. `ls frontend-vite/e2e/*.spec.ts | wc -l` returns 3
2. `grep "desktop" frontend-vite/playwright.config.ts` matches
3. `cd frontend-vite && npx playwright test --project=desktop --reporter=dot` passes
4. `cd frontend-vite && npx playwright test --project=mobile --reporter=dot` passes
</verification>

<success_criteria>
- Playwright installed in frontend-vite with 3 viewport projects
- 3 test files: pages.spec.ts, api-compat.spec.ts, responsiveness.spec.ts
- Tests cover all 4 routes, 6+ API endpoints, and 3 viewports
- All tests pass when stack is running
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-redesign-com-stitch-implementar-designs-stitch-ai-para-elite-racket-catalog-premium-home-quiz-t-cnico-e-analytics-com-responsividade-total/17-04-SUMMARY.md`
</output>
