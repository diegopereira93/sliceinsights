---
phase: 17-ui-redesign-com-stitch
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - frontend/components/home-client.tsx
  - frontend/components/statistics-client.tsx
  - frontend/components/paddle/paddle-card.tsx
autonomous: true
requirements: [UI-01, UI-03]

must_haves:
  truths:
    - "Home hero section has elevated visual design inspired by Stitch output"
    - "PaddleCard uses enhanced glassmorphism and premium styling"
    - "Statistics page tabs and data cards have refined layout"
    - "All existing functionality preserved — filters, comparator, charts"
  artifacts:
    - path: "frontend/components/home-client.tsx"
      provides: "Enhanced home hero section with Stitch-inspired design"
      contains: "motion.div"
    - path: "frontend/components/statistics-client.tsx"
      provides: "Refined statistics layout with improved data presentation"
      contains: "bg-background"
    - path: "frontend/components/paddle/paddle-card.tsx"
      provides: "Premium paddle card with enhanced glassmorphism"
      contains: "glass-card"
  key_links:
    - from: "frontend/components/home-client.tsx"
      to: "frontend/components/paddle/paddle-card.tsx"
      via: "renders PaddleCard in grid"
      pattern: "PaddleCard"
    - from: "frontend/components/statistics-client.tsx"
      to: "frontend/components/statistics/"
      via: "renders statistics sub-components"
      pattern: "from.*@/components/statistics"
---

<objective>
Refine the Home page hero section and PaddleCard visuals, and improve the Statistics page layout and data presentation — both inspired by Stitch AI designs. These pages already use design system tokens correctly, so the work is purely visual enhancement, not migration.

Purpose: While the Home and Statistics pages already follow the design system, they can be visually elevated with refined glassmorphism, better spacing, enhanced card hover states, and improved data presentation layouts inspired by the Stitch designs.

Output: Enhanced home-client.tsx, statistics-client.tsx, and paddle-card.tsx with premium visual refinements.
</objective>

<execution_context>
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/workflows/execute-plan.md
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ui-redesign-com-stitch-implementar-designs-stitch-ai-para-elite-racket-catalog-premium-home-quiz-t-cnico-e-analytics-com-responsividade-total/17-RESEARCH.md
@.planning/phases/17-ui-redesign-com-stitch-implementar-designs-stitch-ai-para-elite-racket-catalog-premium-home-quiz-t-cnico-e-analytics-com-responsividade-total/17-01-SUMMARY.md

<interfaces>
<!-- Home client component — already premium, uses design tokens -->
From frontend/components/home-client.tsx (353 lines):
```typescript
export default function HomeClient({ initialPaddles, brands }: HomeClientProps)
// Uses: motion, AnimatePresence, Button, Card, Badge, cn()
// Hero: motion.div, font-black italic uppercase tracking-tighter
// Cards: PaddleCard component
// Filters: FilterDrawer
```

<!-- Statistics client — 1052 lines, uses tokens correctly -->
From frontend/components/statistics-client.tsx (1052 lines):
```typescript
export function StatisticsClient({ initialPaddles }: StatisticsClientProps)
// Uses: Tabs, TabsContent, TabsList, TabsTrigger from Radix
// Charts: Recharts (DO NOT replace)
// Tokens: bg-background, border-border, text-muted-foreground
```

<!-- PaddleCard — primary display component -->
From frontend/components/paddle/paddle-card.tsx:
Used in Home grid to render each paddle with image, price, specs, store badge

<!-- Design tokens available -->
primary: #ceff00, background: #000000, muted: #111111, border: #222222
glass-card: CSS class from globals.css
shadow-glow: 0 0 20px rgba(206, 255, 0, 0.3)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Home hero section and PaddleCard visual design</name>
  <files>frontend/components/home-client.tsx, frontend/components/paddle/paddle-card.tsx</files>
  <read_first>
    - frontend/components/home-client.tsx (FULL file — 353 lines, current hero and layout)
    - frontend/components/paddle/paddle-card.tsx (FULL file — current card implementation)
    - frontend/app/globals.css (glass-card, glass utility classes)
    - frontend/tailwind.config.js (available shadows, border-radius values)
    - .planning/phases/17-ui-redesign-com-stitch-implementar-designs-stitch-ai-para-elite-racket-catalog-premium-home-quiz-t-cnico-e-analytics-com-responsividade-total/designs/stitch-design-map.md (Stitch design reference)
  </read_first>
  <action>
**PaddleCard enhancements** (`frontend/components/paddle/paddle-card.tsx`):

1. Add hover glow effect: update the card root to include `hover:shadow-glow` transition:
   ```tsx
   className={cn(
     "glass-card border-none overflow-hidden group transition-all duration-300",
     "hover:ring-2 hover:ring-primary/30 hover:shadow-glow hover:-translate-y-1"
   )}
   ```

2. Add subtle motion entrance animation to the card (wrap outer element):
   ```tsx
   <motion.div
     initial={{ opacity: 0, y: 10 }}
     animate={{ opacity: 1, y: 0 }}
     transition={{ duration: 0.3 }}
   >
   ```
   Import `motion` from `framer-motion` if not already imported.

3. Enhance price badge styling — use the premium badge pattern:
   ```tsx
   <Badge className="bg-primary/10 text-primary border border-primary/20 font-black">
     R$ {price}
   </Badge>
   ```

4. Add image skeleton/placeholder for paddles without images:
   ```tsx
   {image_url ? (
     <img ... />
   ) : (
     <div className="w-full h-48 bg-muted rounded-t-xl flex items-center justify-center">
       <span className="text-muted-foreground text-sm">Sem imagem</span>
     </div>
   )}
   ```

**Home hero enhancements** (`frontend/components/home-client.tsx`):

1. Find the hero section (the top area with h1 title and description). Enhance with:
   - Add a subtle gradient overlay: `bg-gradient-to-b from-primary/5 via-background to-background`
   - Add animated counter/stat if not already present (total paddles, total stores)
   - Ensure hero text uses `font-black italic uppercase tracking-tighter` (should already be there)

2. Add a subtle divider between hero and catalog grid:
   ```tsx
   <div className="h-px bg-gradient-to-r from-transparent via-primary/30 to-transparent my-8" />
   ```

3. Do NOT modify any filter logic, comparator logic, or data flow. Only visual changes.

4. Do NOT remove any existing framer-motion animations — only add/enhance.

5. Verify the `motion` import from `framer-motion` exists (it should already in home-client.tsx).
  </action>
  <verify>
    <automated>cd /home/diego/Documentos/projetos/data-products/sliceinsights/frontend && grep -c "hover:shadow-glow\|hover:ring-2\|hover:-translate-y" components/paddle/paddle-card.tsx && grep -c "glass-card" components/paddle/paddle-card.tsx</automated>
  </verify>
  <acceptance_criteria>
    - paddle-card.tsx contains `hover:shadow-glow` or `hover:ring-2 hover:ring-primary/30`
    - paddle-card.tsx contains `glass-card`
    - paddle-card.tsx contains `transition-all` or `transition-colors`
    - home-client.tsx still contains `motion` import from framer-motion
    - home-client.tsx still contains `AnimatePresence`
    - home-client.tsx still contains `FilterDrawer` (filter logic untouched)
    - home-client.tsx still contains `PaddleComparator` (comparator untouched)
    - No hardcoded hex colors added (zero new `bg-[#` or `text-[#` patterns)
  </acceptance_criteria>
  <done>PaddleCard has enhanced glassmorphism with hover glow and lift. Home hero has refined gradient and visual divider. All functionality preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Refine Statistics page layout and data presentation</name>
  <files>frontend/components/statistics-client.tsx</files>
  <read_first>
    - frontend/components/statistics-client.tsx (FULL file — 1052 lines, current layout with Recharts + Tabs)
    - frontend/components/statistics/ (directory listing — understand sub-components available)
    - frontend/tailwind.config.js (design tokens)
    - .planning/phases/17-ui-redesign-com-stitch-implementar-designs-stitch-ai-para-elite-racket-catalog-premium-home-quiz-t-cnico-e-analytics-com-responsividade-total/designs/stitch-design-map.md (Stitch design reference)
  </read_first>
  <action>
**Statistics layout refinements** (`frontend/components/statistics-client.tsx`):

This is a 1052-line file. Changes must be SURGICAL — only visual refinements, NO logic changes.

1. **Tab styling enhancement** — find the `<TabsList>` and ensure it uses premium styling:
   ```tsx
   <TabsList className="bg-muted border border-border rounded-xl p-1">
     <TabsTrigger className="data-[state=active]:bg-primary data-[state=active]:text-primary-foreground rounded-lg font-black text-sm">
   ```
   Check if this is already the pattern — if so, skip.

2. **Card container refinement** — find sections that render data cards (leaderboard, distribution, etc.) and ensure they use:
   ```tsx
   className="glass-card border-none p-6 rounded-2xl"
   ```
   instead of plain `bg-muted` divs (if any exist).

3. **Section headers** — ensure section titles use consistent typography:
   ```tsx
   <h2 className="text-lg font-black text-foreground">
   ```

4. **Chart container spacing** — Recharts containers should have consistent padding:
   ```tsx
   <div className="bg-muted/50 rounded-xl p-4 border border-border/50">
     {/* Recharts component */}
   </div>
   ```

5. **Add subtle motion** to tab content transitions (if not already present):
   ```tsx
   <TabsContent value="overview" className="mt-4">
     <motion.div
       initial={{ opacity: 0, y: 10 }}
       animate={{ opacity: 1, y: 0 }}
       transition={{ duration: 0.2 }}
     >
       {/* tab content */}
     </motion.div>
   </TabsContent>
   ```
   Only add this if `motion` is already imported (check first). If framer-motion is not imported, add the import.

6. **DO NOT modify Recharts components** — do not change chart types, data sources, axes, or any Recharts props. Only modify the container/wrapper styling around charts.

7. **DO NOT change any data fetching logic**, API calls, or state management.

8. Run the E2E smoke test after changes:
   ```bash
   cd /home/diego/Documentos/projetos/data-products/sliceinsights/frontend
   BASE_URL=http://localhost:3002 npx playwright test e2e/sliceinsights.spec.ts --project=desktop --reporter=list 2>&1 | tail -5
   ```
  </action>
  <verify>
    <automated>cd /home/diego/Documentos/projetos/data-products/sliceinsights/frontend && grep -c "glass-card\|rounded-2xl\|rounded-xl" components/statistics-client.tsx && grep -c "bg-\[#" components/statistics-client.tsx</automated>
  </verify>
  <acceptance_criteria>
    - statistics-client.tsx contains `glass-card` or `rounded-2xl` (at least 1 occurrence)
    - statistics-client.tsx contains zero `bg-[#` hardcoded hex colors (should already be 0 — verify no regressions)
    - statistics-client.tsx still contains `TabsList` and `TabsTrigger` (Radix Tabs preserved)
    - statistics-client.tsx still contains `Recharts` or `ResponsiveContainer` or chart imports
    - statistics-client.tsx still exports `StatisticsClient` function
    - `npx playwright test e2e/sliceinsights.spec.ts --project=desktop` passes
  </acceptance_criteria>
  <done>Statistics page has refined tab styling, enhanced card containers, and consistent section headers. All Recharts and data logic untouched. E2E tests pass.</done>
</task>

</tasks>

<verification>
1. PaddleCard has hover glow and enhanced glassmorphism
2. Home hero has gradient and visual refinements
3. Statistics has refined tabs and card containers
4. Zero new hardcoded hex colors introduced
5. E2E smoke test passes: `npx playwright test e2e/sliceinsights.spec.ts --project=desktop`
</verification>

<success_criteria>
- Home hero section visually elevated with gradient and divider
- PaddleCard has premium hover states (glow, lift, ring)
- Statistics tabs and data cards use refined styling
- Zero logic changes — all filters, comparator, charts, data flow preserved
- No new hardcoded hex colors
- E2E tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-redesign-com-stitch-implementar-designs-stitch-ai-para-elite-racket-catalog-premium-home-quiz-t-cnico-e-analytics-com-responsividade-total/17-03-SUMMARY.md`
</output>
