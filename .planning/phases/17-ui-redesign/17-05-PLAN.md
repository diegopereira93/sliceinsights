---
phase: 17-ui-redesign
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-vite/src/lib/types/paddle.ts
  - frontend-vite/src/components/WeightSensationScale.tsx
  - frontend-vite/src/components/PaddleCard.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - Card gamma replica as informações completas do card original
    - Usuário pode ver avaliação com 5 estrelas
    - Usuário pode ver peso (swing weight) e sensação de peso
    - Card inclui barras de Power e Control com ícones
  artifacts:
    - path: "frontend-vite/src/components/PaddleCard.tsx"
      provides: "Card de catálogo completo replicando o original"
      min_lines: 180
    - path: "frontend-vite/src/components/WeightSensationScale.tsx"
      provides: "Componente de sensação de peso"
    - path: "frontend-vite/src/lib/types/paddle.ts"
      provides: "Tipos atualizados com campos rating e weight"
---

<objective>
Replicar o card de catálogo completo do site original (sliceinsights.vercel.app) no projeto gamma (frontend-vite), incluindo: marca como badge, avaliação 5 estrelas, peso, barras de Power/Control com ícones, e sensação de peso.
</objective>

<execution_context>
@/home/diego/Documentos/projetos/data-products/sliceinsights/.opencode/get-shit-done/workflows/execute-plan.md
@/home/diego/Documentos/projetos/data-products/sliceinsights/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend-old/components/paddle/paddle-card.tsx — Card original com todas as informações
@frontend-old/components/ui/weight-sensation-scale.tsx — Componente de sensação de peso
@frontend-old/types/paddle.ts — Tipos originais do Paddle
@frontend-vite/src/components/PaddleCard.tsx — Card atual do gamma (incompleto)
@frontend-vite/src/lib/types/paddle.ts — Tipos atuais do gamma

**Diferenças identificadas:**
- Gamma: falta rating (avaliação 5 estrelas), falta weight (peso), falta weightSensationScale
- Gamma: StatBar tem barras mas sem ícones Zap/Shield, preço sem "A partir de"
- Gamma: imagem menor (h-48 vs aspect-[4/5] original)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar tipos Paddle no gamma</name>
  <files>frontend-vite/src/lib/types/paddle.ts</files>
  <action>
Adicionar campos necessários ao tipo Paddle:
- `rating?: number` — avaliação 0-5
- `weight?: string` — peso como string (ex: "7.8 oz")
- Manter `swingWeight?: number` que já existe
- Manter `weightSensation?: string` e `weightSensationDescription?: string` que já existem
  </action>
  <verify>
    <automated>grep -q "rating" frontend-vite/src/lib/types/paddle.ts && grep -q "weight\?" frontend-vite/src/lib/types/paddle.ts</automated>
  </verify>
  <done>Tipo Paddle atualizado com campos rating e weight</done>
</task>

<task type="auto">
  <name>Task 2: Criar componente WeightSensationScale</name>
  <files>frontend-vite/src/components/WeightSensationScale.tsx</files>
  <action>
Criar componente replicando o original de frontend-old/components/ui/weight-sensation-scale.tsx:
- Props: swingWeight (number | null | undefined), className?: string
- Se swingWeight é null/undefined: mostrar "Dados indisponíveis" com HelpCircle
- Se swingWeight < 110: "Parece uma Pena" (Feather, emerald)
- Se swingWeight > 120: "Cabeça Pesada" (Hammer, rose)
- Se swingWeight >= 115: "Manuseio Firme" (Scale, amber)
- Se 110-114: "Equilibrada" (Scale, blue)
- Mostrar label + descrição + barra de progresso + "SW {valor}"
Usar ícones de lucide-react (Feather, Scale, Hammer, HelpCircle)
  </action>
  <verify>
    <automated>ls frontend-vite/src/components/WeightSensationScale.tsx</automated>
  </verify>
  <done>Componente WeightSensationScale criado e exportado</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar PaddleCard com todas as informações do original</name>
  <files>frontend-vite/src/components/PaddleCard.tsx</files>
  <action>
Replicar o card completo do original (frontend-old/components/paddle/paddle-card.tsx):

1. **Layout**: Usar aspect ratio similar ao original ou h-56 (maior que h-48 atual)

2. **Marca**: Renderizar como Badge acima do título, igual ao original
   ```tsx
   <span className="text-[10px] font-bold text-primary tracking-widest uppercase">{paddle.brand}</span>
   ```

3. **Espessura**: Renderizar como Badge (já tem mas mover para próximo do título)

4. **Avaliação 5 estrelas**: Adicionar acima/subtítulo com Star icons
   ```tsx
   <div className="flex items-center gap-1">
     {[...Array(5)].map((_, i) => (
       <Star key={i} className={`w-3 h-3 ${i < Math.floor(paddle.rating ?? 0) ? 'text-primary fill-primary' : 'text-zinc-600'}`} />
     ))}
     {paddle.rating && <span className="text-xs text-zinc-400 ml-1">{paddle.rating.toFixed(1)}</span>}
   </div>
   ```

5. **Barras Power/Control**: Adicionar ícones Zap (Power) e Shield (Control)
   - StatBar atual já tem barras, mas adicionar ícone antes do label
   - Estilo: Zap (powerScore), Shield (controlScore)

6. **Sensação de Peso**: Importar e usar WeightSensationScale
   ```tsx
   <WeightSensationScale swingWeight={paddle.swingWeight} />
   ```

7. **Preço**: Mudar para "A partir de R$ X,XX"

8. **Botão**: Manter "BATALHA" mas renomear para "COMPARAR" (funcionalidade de comparação existente)

Manter todas as funcionalidades existentes: BattleContext, link COMPRAR, animações framer-motion
  </action>
  <verify>
    <automated>npm run build --prefix frontend-vite 2>&1 | tail -20</automated>
  </verify>
  <done>Card completo com marca, avaliação 5 estrelas, barras Power/Control com ícones, WeightSensation, preço "A partir de"</done>
</task>

</tasks>

<verification>
Verificar visualmente que o card gamma agora inclui:
- Imagem do produto (maior)
- Marca como badge/título pequeno
- Espessura como badge
- Avaliação com 5 estrelas
- Peso (swingWeight)
- Barra de Power com ícone Zap + número
- Barra de Control com ícone Shield + número
- Sensação de Peso (WeightSensationScale)
- Preço "A partir de R$ X,XX"
- Botão "COMPARAR" (ou "BATALHA" mantendo função)
</verification>

<success_criteria>
Card gamma replica todas as informações do card original: marca, avaliação, peso, barras Power/Control com ícones, sensação de peso, preço com "A partir de"
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-redesign/17-05-SUMMARY.md`
</output>
