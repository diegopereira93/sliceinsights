---
plan_id: 06-04
wave: 4
depends_on:
  - 06-02
files_modified:
  - scripts/scraper_utils.py
  - scripts/run_scraper.py
requirements:
  - SLO-01
  - SLO-05
autonomous: true
---

Integrate real-time SLO validation into the scraper execution flow.

## Tasks

<task>
<action>
Modify the scraper execution entry point (likely `scripts/run_scraper.py` or individual scraper `main()` functions) to call `validate_job_slo(scraper_name)` after successful data commit.
Wrap the call in a `try/except` block to ensure it is non-blocking.
</action>
<read_first>
- scripts/run_scraper.py (if it exists, else check a specific scraper like `scripts/scrapers/mercado_livre.py`)
- scripts/scraper_utils.py (to see where common post-run logic might be)
- .planning/phases/06-slo-enforcement-validation/06-RESEARCH.md (Real-time Integration section)
</read_first>
<acceptance_criteria>
- The validator is called after the data is committed.
- Scraper execution does NOT fail if the SLO validator fails.
- Trace shows "SLO validation triggered for [scraper_name]".
</acceptance_criteria>
</task>

<task>
<action>
Update `scripts/scraper_utils.py` if needed to provide a unified `finish_run()` function that includes SLO validation.
</action>
<read_first>
- scripts/scraper_utils.py
</read_first>
<acceptance_criteria>
- `finish_run` exists and includes a call to `slo_validator.validate_job_slo`.
</acceptance_criteria>
</task>

<task>
<action>
Verify real-time validation by running a sample scraper and checking the `slo_logs` table.
Command: `python scripts/run_scraper.py mercado_livre` (or equivalent).
</action>
<read_first>
- .planning/phases/06-slo-enforcement-validation/06-RESEARCH.md (Validation Architecture section)
</read_first>
<acceptance_criteria>
- `psql $DATABASE_URL_SYNC -c "SELECT * FROM slo_logs WHERE scraper_name = 'mercado_livre' ORDER BY checked_at DESC LIMIT 1;"` returns a row.
</acceptance_criteria>
</task>

## Must Haves
- Real-time SLO check after every individual scraper run.
- Non-blocking execution pattern to protect data ingestion.
