---
plan_id: 06-02
wave: 2
depends_on:
  - 06-01
files_modified:
  - scripts/slo_validator.py
requirements:
  - SLO-03
  - SLO-04
  - SLO-05
autonomous: true
---

Develop the core SLO validation engine that contains the logic for checking freshness and completeness.

## Tasks

<task>
<action>
Create `scripts/slo_validator.py` shell with database session utility.
Reuse session logic from `scripts/scraper_utils.py`.
</action>
<read_first>
- scripts/scraper_utils.py
- .planning/phases/06-slo-enforcement-validation/06-RESEARCH.md
</read_first>
<acceptance_criteria>
- `scripts/slo_validator.py` exists and can connect to the database.
- It imports `SLOLog` from `app.models.slo`.
</acceptance_criteria>
</task>

<task>
<action>
Implement `check_freshness(session, scraper_name=None)` in `scripts/slo_validator.py`.
It should query `market_offers` table for the latest `last_updated` per scraper.
If `last_updated` > 24h, it should record a "fail" in `slo_logs`.
</action>
<read_first>
- app/models/market_offer.py (check field name: `last_updated`)
- scripts/measure_freshness.py (reference implementation)
- .planning/phases/06-slo-enforcement-validation/06-RESEARCH.md (Query pattern)
</read_first>
<acceptance_criteria>
- `check_freshness` implementation uses `last_updated` column and `FRESHNESS_SLO_HOURS`.
- It handles the case where `scraper_name` is None (checks all scrapers).
- It writes results to `slo_logs` table.
</acceptance_criteria>
</task>

<task>
<action>
Implement `check_completeness(session)` in `scripts/slo_validator.py`.
It should query `paddle_master` table for the latest `updated_at`.
If max age > 7 days (168h), it should record a "fail" in `slo_logs`.
</action>
<read_first>
- app/models/paddle.py (check field name: `updated_at`)
- .planning/phases/06-slo-enforcement-validation/06-RESEARCH.md (Query pattern)
</read_first>
<acceptance_criteria>
- `check_completeness` implementation uses `updated_at` column and `COMPLETENESS_SLO_HOURS`.
- It writes results (pass/fail) to `slo_logs` table with `metric_type='completeness'`.
</acceptance_criteria>
</task>

<task>
<action>
Implement CLI interface in `scripts/slo_validator.py` with `--all` and `--scraper <NAME>` arguments.
Include a `validate_job_slo(scraper_name)` wrapper function for real-time hooks.
</action>
<read_first>
- .planning/phases/06-slo-enforcement-validation/06-RESEARCH.md (Real-time Integration Pattern)
</read_first>
<acceptance_criteria>
- `python scripts/slo_validator.py --all` executes both checks.
- `validate_job_slo("test")` can be called from other scripts.
</acceptance_criteria>
</task>

## Must Haves
- Functional `check_freshness` and `check_completeness` methods.
- Integration with `slo_logs` table for result persistence.
- Support for both individual scraper checks and global audits.
