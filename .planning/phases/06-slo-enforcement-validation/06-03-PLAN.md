---
plan_id: 06-03
wave: 3
depends_on:
  - 06-02
files_modified:
  - .github/workflows/slo-check.yml
requirements:
  - SLO-02
  - SLO-05
autonomous: true
---

Set up the scheduled GitHub Actions workflow to run SLO validation every 6 hours.

## Tasks

<task>
<action>
Create `.github/workflows/slo-check.yml`.
Configure `on.schedule` with cron `'0 */6 * * *'`.
Include `workflow_dispatch` for manual triggers.
</action>
<read_first>
- .github/workflows/ci.yml (for boilerplate)
- .planning/phases/06-slo-enforcement-validation/06-RESEARCH.md (GitHub Actions section)
</read_first>
<acceptance_criteria>
- `.github/workflows/slo-check.yml` exists with the correct cron schedule.
- `on: workflow_dispatch: {}` is present.
</acceptance_criteria>
</task>

<task>
<action>
Define the job in `slo-check.yml`.
Steps: checkout, setup-python, install deps, and run validator.
Command: `python scripts/slo_validator.py --all`.
Inject `DATABASE_URL_SYNC` from secrets.
</action>
<read_first>
- .github/workflows/ci.yml (reference environment variables)
</read_first>
<acceptance_criteria>
- Job uses `DATABASE_URL_SYNC: ${{ secrets.DATABASE_URL_SYNC }}` (or appropriate secret from CI env).
- Step calls `python scripts/slo_validator.py --all`.
</acceptance_criteria>
</task>

<task>
<action>
Run checks on the workflow file by simulating locally or verifying syntax.
</action>
<read_first>
- .github/workflows/slo-check.yml
</read_first>
<acceptance_criteria>
- Workflow file is valid YAML.
- Workflow action paths are correct.
</acceptance_criteria>
</task>

## Must Haves
- Automated SLO validation running 4x daily.
- Manual trigger capability for ad-hoc validation.
- Secure database connection via GitHub Secrets.
