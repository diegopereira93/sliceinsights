---
plan_id: 06-01
wave: 1
depends_on: []
files_modified:
  - app/models/slo.py
  - app/db/database.py
  - scripts/slo_config.py
requirements:
  - SLO-05
autonomous: true
---

Implement the database model for SLO logging and the centralized configuration for SLO thresholds.

## Tasks

<task>
<action>
Create `app/models/slo.py` with the `SLOLog` SQLModel class.
Include fields: `id` (PK), `scraper_name` (str), `metric_type` (str: freshness/completeness), `value_hours` (float), `threshold_hours` (float), `status` (str: pass/fail), `checked_at` (datetime), and `details` (JSONB).
</action>
<read_first>
- app/models/paddle.py (for timestamp pattern)
- .planning/phases/06-slo-enforcement-validation/06-RESEARCH.md (for schema definition)
</read_first>
<acceptance_criteria>
- `app/models/slo.py` exists and contains `class SLOLog(SQLModel, table=True)`.
- `SLOLog` contains a `details` field using `JSONB` from `sqlalchemy.dialects.postgresql`.
- File passes linting (if run manually).
</acceptance_criteria>
</task>

<task>
<action>
Import `SLOLog` in `app/db/database.py` or wherever models are registered to ensure it is picked up by SQLModel/Alembic.
</action>
<read_first>
- app/db/database.py
- .planning/phases/06-slo-enforcement-validation/06-RESEARCH.md (Migration approach section)
</read_first>
<acceptance_criteria>
- `from app.models.slo import SLOLog` is present in `app/db/database.py` (or the central model registry).
</acceptance_criteria>
</task>

<task>
<action>
Generate and run an Alembic migration to create the `slo_logs` table.
Command: `alembic revision --autogenerate -m "add slo_logs table"` followed by `alembic upgrade head`.
</action>
<read_first>
- alembic/env.py
</read_first>
<acceptance_criteria>
- A new file `alembic/versions/*_add_slo_logs_table.py` exists.
- `psql $DATABASE_URL_SYNC -c "\d slo_logs"` shows the table created with correct columns.
</acceptance_criteria>
</task>

<task>
<action>
Create `scripts/slo_config.py` with SLO constants.
Define: `FRESHNESS_SLO_HOURS = 24` and `COMPLETENESS_SLO_DAYS = 7`.
Include `COMPLETENESS_SLO_HOURS = COMPLETENESS_SLO_DAYS * 24`.
</action>
<read_first>
- .planning/phases/06-slo-enforcement-validation/06-RESEARCH.md (SLO Thresholds section)
</read_first>
<acceptance_criteria>
- `scripts/slo_config.py` exists.
- `FRESHNESS_SLO_HOURS` is set to 24.
- `COMPLETENESS_SLO_HOURS` is set to 168.
</acceptance_criteria>
</task>

## Must Haves
- `slo_logs` table created in PostgreSQL with JSONB support.
- Centralized `slo_config.py` for easy threshold adjustments.
