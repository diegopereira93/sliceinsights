---
plan_id: 06-05
wave: 5
depends_on:
  - 06-03
  - 06-04
files_modified:
  - docs/slo-guide.md
  - .planning/phases/06-slo-enforcement-validation/06-SUMMARY.md
requirements:
  - SLO-01
  - SLO-02
  - SLO-03
  - SLO-04
  - SLO-05
autonomous: true
---

Verification of all SLO requirements, breach simulation, and creation of the SLO operator guide.

## Tasks

<task>
<action>
Simulate a Freshness SLO breach by updating a `market_offers` record's `updated_at` (or `last_updated`) to <NOW - 25 hours>.
Run the validator: `python scripts/slo_validator.py --all`.
Verify a "fail" status is logged.
</action>
<read_first>
- scripts/slo_validator.py
- scripts/slo_config.py
</read_first>
<acceptance_criteria>
- A row exists in `slo_logs` with `metric_type='freshness'`, `status='fail'`, and `value_hours` > 24.
- `psql $DATABASE_URL_SYNC -c "SELECT status FROM slo_logs WHERE metric_type='freshness' AND status='fail' LIMIT 1;"`
</acceptance_criteria>
</task>

<task>
<action>
Simulate a Completeness SLO breach by updating a `paddle_master` record's `updated_at` to <NOW - 8 days>.
Run the validator: `python scripts/slo_validator.py --all`.
Verify a "fail" status is logged.
</action>
<read_first>
- scripts/slo_validator.py
- scripts/slo_config.py
</read_first>
<acceptance_criteria>
- A row exists in `slo_logs` with `metric_type='completeness'`, `status='fail'`, and `value_hours` > 168.
- `psql $DATABASE_URL_SYNC -c "SELECT status FROM slo_logs WHERE metric_type='completeness' AND status='fail' LIMIT 1;"`
</acceptance_criteria>
</task>

<task>
<action>
Create `docs/slo-guide.md` documenting the SLO thresholds, how to check logs, and how the automated checks work.
Include example SQL queries for debugging `slo_logs`.
</action>
<read_first>
- .planning/phases/06-slo-enforcement-validation/06-RESEARCH.md
</read_first>
<acceptance_criteria>
- `docs/slo-guide.md` exists.
- It contains the thresholds (24h/7d) and the `psql` query for breaches.
</acceptance_criteria>
</task>

<task>
<action>
Create `06-SUMMARY.md` in the `.planning/phases/06-slo-enforcement-validation/` directory to close the phase.
Summarize the results of the simulations and the status of requirements.
</action>
<read_first>
- .planning/REQUIREMENTS.md
</read_first>
<acceptance_criteria>
- `06-SUMMARY.md` exists and lists all requirements from SLO-01 to SLO-05 as completed.
</acceptance_criteria>
</task>

## Must Haves
- End-to-end verification of breach detection.
- Documentation for future maintainers.
- Updated project state reflecting phase completion.
