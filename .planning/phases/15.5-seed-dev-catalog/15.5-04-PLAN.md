---
phase: 15.5-seed-dev-catalog
plan: "04"
type: execute
wave: 2
depends_on: [15.5-03]
files_modified: []
autonomous: false
requirements: []
---

<objective>
Verificar que o frontend mostra paddles e o fluxo de recomendação funciona.
</objective>

## Tarefas

### 1. Verificar API returns data
```bash
curl -s "http://localhost:8002/api/v1/paddles?available_in_brazil=true&limit=3" | python3 -c "
import sys, json
d = json.load(sys.stdin)
paddles = d.get('data', [])
print(f'Paddles: {len(paddles)}')
if paddles:
    p = paddles[0]
    print(f'First: {p.get(\"brand_name\")} {p.get(\"model_name\")}')
"
```

### 2. Playwright verification
```bash
playwright-cli open http://localhost:3002
# Take snapshot, check for paddle cards
# Check home loads paddle content (not blank)
```

```bash
playwright-cli snapshot
# Save snapshot
```

### 3. Check /recommend page
```bash
playwright-cli goto http://localhost:3002/recommend
playwright-cli snapshot
# Check wizard step 1 loads
```

## must_haves

- truths:
  - Home mostra >= 3 paddle cards (não blank)
  - /recommend carrega e mostra step 1 do wizard
  - API /paddles retorna >= 3 paddles

## artifacts

- `.playwright-cli/page-*-seed-verify.yml` — snapshot da home com paddles
