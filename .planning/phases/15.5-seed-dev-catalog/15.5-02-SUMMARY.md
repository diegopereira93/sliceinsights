---
phase: 15.5-seed-dev-catalog
plan: "02"
executed: "2026-03-22"
outcome: partial
---

## Outcome

Added `--ingest` flag to `scrape_justpaddles.py` with market-offer ingestion via Playwright. However:
- Just Paddles domain (justpaddles.com.br) is **unreachable** from the container network (ERR_NAME_NOT_RESOLVED)
- Scraper runs but finds 0 products
- Installed Chromium deps (libcairo2) in backend container to fix playwright runtime deps

**Partial success**: The code change is correct, but the store is inaccessible from this environment. The `run_market_offer_ingestion()` function works — just has no data to ingest.

## Also Fixed

- `scripts/run_scraper.py` — changed `sys.path` from `/app/scripts` to `/app` (fixes `ModuleNotFoundError: No module named 'scripts'` for all scrapers)
- Installed system deps for Playwright Chromium: `apt-get install libcairo2` (+ other libs)

## Artifacts

- `scripts/scrape_justpaddles.py` — modified with `--ingest` flag
- `scripts/run_scraper.py` — sys.path fix
