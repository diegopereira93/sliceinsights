---
phase: 15.5-seed-dev-catalog
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: []
---

<objective>
Criar script `scripts/seed_stores.py` que limpa stores stale e upsert as 10 stores com nomes exatos que os scrapers esperam.
</objective>

## Problema
DB local tem stores com nomes errados (Test Store, Pickleball Central, PB Village, Net2Court). Todos os scrapers fazem `Store.name == STORE_NAME` via `.one()` — crasham com `NoResultFound`.

Stores corretas esperadas pelos scrapers:

| Store Name | Slug | Base URL |
|---|---|---|
| Joola Brasil | joola-brasil | https://joola.com.br |
| Shark | shark | https://sharkbeachtennis.com.br |
| Loja Supremo | loja-supremo | https://lojasupremo.com.br |
| yoSports | yosports | https://yosports.com.br |
| PCKL House | pcklhouse | https://pcklhouse.com.br |
| ProPadel | propadel | https://lojapropadel.com.br |
| Just Paddles | just-paddles | https://justpaddles.com.br |
| Brazil Pickleball Store | brazil-pickleball-store | https://brazilpickleballstore.com.br |
| Drop Shot Brasil | drop-shot-brasil | https://dropshot.com.br |
| ProSpin | prospin | https://prospin.com.br |

## Tarefas

### 1. Criar `scripts/seed_stores.py`
```python
"""
Seed the 10 correct Store records into the database.
Usage: python scripts/seed_stores.py
"""
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent.parent))

from sqlmodel import Session, select

from app.db.database import sync_engine
from app.models.store import Store

STORES = [
    {"name": "Joola Brasil",           "slug": "joola-brasil",              "base_url": "https://joola.com.br"},
    {"name": "Shark",                   "slug": "shark",                     "base_url": "https://sharkbeachtennis.com.br"},
    {"name": "Loja Supremo",             "slug": "loja-supremo",               "base_url": "https://lojasupremo.com.br"},
    {"name": "yoSports",                 "slug": "yosports",                   "base_url": "https://yosports.com.br"},
    {"name": "PCKL House",               "slug": "pcklhouse",                 "base_url": "https://pcklhouse.com.br"},
    {"name": "ProPadel",                 "slug": "propadel",                   "base_url": "https://lojapropadel.com.br"},
    {"name": "Just Paddles",             "slug": "just-paddles",               "base_url": "https://justpaddles.com.br"},
    {"name": "Brazil Pickleball Store",  "slug": "brazil-pickleball-store",    "base_url": "https://brazilpickleballstore.com.br"},
    {"name": "Drop Shot Brasil",         "slug": "drop-shot-brasil",           "base_url": "https://dropshot.com.br"},
    {"name": "ProSpin",                  "slug": "prospin",                    "base_url": "https://prospin.com.br"},
]

STALE_NAMES = {
    "Test Store", "Pickleball Central", "PB Village", "Net2Court",
    "JustPaddles", "ProPadel", "propadel", "justpaddles",
}

def main():
    with Session(sync_engine) as session:
        # 1. Delete stale stores
        stale = session.exec(
            select(Store).where(Store.name.in_(STALE_NAMES))
        ).all()
        for s in stale:
            print(f"🗑️  Deleting stale store: {s.name} (id={s.id})")
            session.delete(s)
        session.commit()

        # 2. Upsert correct stores
        for data in STORES:
            existing = session.exec(
                select(Store).where(Store.name == data["name"])
            ).first()
            if existing:
                print(f"✅  Store exists: {data['name']}")
                continue
            store = Store(**data)
            session.add(store)
            print(f"➕  Created store: {data['name']}")

        session.commit()

        # 3. Verify
        all_stores = session.exec(select(Store)).all()
        print(f"\n📦 Total stores in DB: {len(all_stores)}")
        for s in all_stores:
            print(f"   - {s.name} ({s.base_url})")

if __name__ == "__main__":
    main()
```

### 2. Executar seed
```bash
docker exec picklematch_api_v3 python scripts/seed_stores.py
```

## must_haves

- truths:
  - `scripts/seed_stores.py` criado e executável
  - Após executar, DB tem exatamente 10 stores com nomes corretos
  - Nenhuma store stale permanece (Test Store, Pickleball Central, PB Village, Net2Court deletadas)
  - Script loga cada operação (delete/create)

## artifacts

- `scripts/seed_stores.py`
