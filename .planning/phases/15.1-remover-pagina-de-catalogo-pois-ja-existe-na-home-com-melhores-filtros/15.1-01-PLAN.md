---
phase: 15.1-remover-pagina-de-catalogo-pois-ja-existe-na-home-com-melhores-filtros
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/catalog/page.tsx
  - frontend/app/catalog/catalog-client.tsx
  - frontend/components/catalog/catalog-filter-bar.tsx
  - frontend/components/catalog/catalog-grid.tsx
  - frontend/components/catalog/catalog-paddle-card.tsx
  - frontend/components/catalog/catalog-pagination.tsx
  - frontend/types/catalog.ts
  - frontend/components/ui/bottom-nav.tsx
  - frontend/app/sitemap.ts
  - frontend/next.config.mjs
  - frontend/components/paddle/filter-drawer.tsx
  - frontend/e2e/sliceinsights.spec.ts
autonomous: true
requirements:
  - CLEANUP-01
  - CLEANUP-02
  - CLEANUP-03
  - CLEANUP-04
  - CLEANUP-05

must_haves:
  truths:
    - "Rota /catalog nao existe mais — retorna redirect 301 para /"
    - "Bottom-nav mostra 'IA' apontando para /recommend em vez de 'Catalogo' apontando para /catalog"
    - "Sitemap nao contem entrada /catalogo"
    - "Build TypeScript passa sem erros"
    - "Home page continua funcional com filtros de raquetes"
    - "FilterDrawer nao tem props orfas de loja (storeFilter/onStoreChange/stores removidos)"
  artifacts:
    - path: "frontend/components/ui/bottom-nav.tsx"
      provides: "NavItem IA apontando para /recommend"
      contains: "/recommend"
    - path: "frontend/next.config.mjs"
      provides: "Redirect permanente /catalog -> /"
      contains: "redirects"
    - path: "frontend/app/sitemap.ts"
      provides: "Sitemap sem entrada /catalogo"
  key_links:
    - from: "frontend/components/ui/bottom-nav.tsx"
      to: "/recommend"
      via: "NavItem href"
      pattern: "href=\"/recommend\""
    - from: "frontend/next.config.mjs"
      to: "/"
      via: "redirects config"
      pattern: "source.*catalog.*destination.*/"
---

<objective>
Remover completamente a pagina /catalog do frontend Next.js e todos os artefatos exclusivos a ela, pois a home page (/) ja oferece capacidade de filtro equivalente e superior em UX.

Purpose: Eliminar codigo morto e superficie de manutencao desnecessaria. A home tem busca por texto, comparador, quiz e filtros que cobrem o caso de uso do catalogo.
Output: Frontend limpo sem rota /catalog, com redirect 301 para bookmarks existentes, e bottom-nav atualizado com link para /recommend (funcionalidade de alto valor).
</objective>

<execution_context>
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/workflows/execute-plan.md
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-remover-pagina-de-catalogo-pois-ja-existe-na-home-com-melhores-filtros/15.1-RESEARCH.md
@.planning/phases/15.1-remover-pagina-de-catalogo-pois-ja-existe-na-home-com-melhores-filtros/15.1-VALIDATION.md

<interfaces>
<!-- Key files and their current state for the executor -->

From frontend/components/ui/bottom-nav.tsx (line 4, 16):
```tsx
import { Home, Heart, BarChart2, ShoppingBag } from 'lucide-react';
// line 16:
<NavItem href="/catalog" icon={ShoppingBag} label="Catalogo" active={pathname === '/catalog'} />
```

From frontend/app/sitemap.ts (lines 19-24):
```tsx
{
    url: `${baseUrl}/catalogo`,
    lastModified: new Date(),
    changeFrequency: 'daily',
    priority: 0.8,
},
```

From frontend/next.config.mjs (current — no redirects block):
```js
const nextConfig = {
    ...(process.env.NODE_ENV === 'production' && { output: 'standalone' }),
    images: { remotePatterns: [...] },
};
```

From frontend/components/paddle/filter-drawer.tsx (lines 33-35, 45-46):
```tsx
// Props interface includes optional store props:
storeFilter?: string;
onStoreChange?: (slug: string) => void;
stores?: { name: string; slug: string }[];
// Destructured with defaults:
storeFilter = "all", onStoreChange,
stores = []
```

From frontend/e2e/sliceinsights.spec.ts:
```tsx
// Line 4: comment mentions "Catalog"
// Line 37: test.describe('Catalog', ...) — navigates to '/' not '/catalog'
// Line 146: text locator includes "Catálogo" — still matches home page text
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deletar artefatos exclusivos do /catalog e atualizar navegacao</name>
  <files>
    frontend/app/catalog/page.tsx,
    frontend/app/catalog/catalog-client.tsx,
    frontend/components/catalog/catalog-filter-bar.tsx,
    frontend/components/catalog/catalog-grid.tsx,
    frontend/components/catalog/catalog-paddle-card.tsx,
    frontend/components/catalog/catalog-pagination.tsx,
    frontend/types/catalog.ts,
    frontend/components/ui/bottom-nav.tsx,
    frontend/app/sitemap.ts,
    frontend/next.config.mjs
  </files>
  <read_first>
    frontend/components/ui/bottom-nav.tsx,
    frontend/app/sitemap.ts,
    frontend/next.config.mjs,
    frontend/app/catalog/page.tsx,
    frontend/types/catalog.ts
  </read_first>
  <action>
    STEP 1 — Delete 7 files (all exclusive to /catalog):
    - `rm frontend/app/catalog/page.tsx`
    - `rm frontend/app/catalog/catalog-client.tsx`
    - `rmdir frontend/app/catalog/` (directory should be empty after above)
    - `rm frontend/components/catalog/catalog-filter-bar.tsx`
    - `rm frontend/components/catalog/catalog-grid.tsx`
    - `rm frontend/components/catalog/catalog-paddle-card.tsx`
    - `rm frontend/components/catalog/catalog-pagination.tsx`
    - `rmdir frontend/components/catalog/` (directory should be empty after above)
    - `rm frontend/types/catalog.ts`

    STEP 2 — Modify `frontend/components/ui/bottom-nav.tsx`:
    - Line 4: Replace `ShoppingBag` with `Sparkles` in the lucide-react import:
      FROM: `import { Home, Heart, BarChart2, ShoppingBag } from 'lucide-react';`
      TO:   `import { Home, Heart, BarChart2, Sparkles } from 'lucide-react';`
    - Line 16: Replace the catalog NavItem with recommend NavItem:
      FROM: `<NavItem href="/catalog" icon={ShoppingBag} label="Catalogo" active={pathname === '/catalog'} />`
      TO:   `<NavItem href="/recommend" icon={Sparkles} label="IA" active={pathname === '/recommend'} />`

    STEP 3 — Modify `frontend/app/sitemap.ts`:
    - Remove lines 19-24 (the `/catalogo` entry object and its trailing comma):
      DELETE the block:
      ```
      {
          url: `${baseUrl}/catalogo`,
          lastModified: new Date(),
          changeFrequency: 'daily',
          priority: 0.8,
      },
      ```
    - Add a `/recommend` entry in its place (same structure, priority 0.8, changeFrequency 'weekly')

    STEP 4 — Modify `frontend/next.config.mjs`:
    - Add a `redirects` async function to nextConfig for SEO and bookmark preservation:
      Add BEFORE the `images` key:
      ```js
      async redirects() {
          return [
              { source: '/catalog', destination: '/', permanent: true },
              { source: '/catalogo', destination: '/', permanent: true },
          ];
      },
      ```
      This handles both `/catalog` (the actual old route) and `/catalogo` (the URL that was in the sitemap).
  </action>
  <verify>
    <automated>cd /home/diego/Documentos/projetos/data-products/sliceinsights/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ls frontend/app/catalog/ 2>&1 | grep "No such file"` — catalog route directory gone
    - `ls frontend/components/catalog/ 2>&1 | grep "No such file"` — catalog components directory gone
    - `ls frontend/types/catalog.ts 2>&1 | grep "No such file"` — catalog types file gone
    - `grep -c "ShoppingBag" frontend/components/ui/bottom-nav.tsx` returns 0 — old icon removed
    - `grep -c "/catalog" frontend/components/ui/bottom-nav.tsx` returns 0 — old nav link removed
    - `grep -c "Sparkles" frontend/components/ui/bottom-nav.tsx` returns 1 — new icon present
    - `grep -c "/recommend" frontend/components/ui/bottom-nav.tsx` returns 1 — new nav link present
    - `grep -c "catalogo" frontend/app/sitemap.ts` returns 0 — old sitemap entry removed
    - `grep -c "recommend" frontend/app/sitemap.ts` returns at least 1 — new sitemap entry added
    - `grep -c "redirects" frontend/next.config.mjs` returns at least 1 — redirect config present
    - `grep -c "source.*catalog" frontend/next.config.mjs` returns at least 1 — redirect rule present
    - `cd frontend && npx tsc --noEmit` exits with code 0 — TypeScript compiles clean
  </acceptance_criteria>
  <done>
    Todos os 7 arquivos exclusivos do /catalog deletados. Bottom-nav aponta para /recommend com icone Sparkles e label "IA". Sitemap nao contem /catalogo e contem /recommend. next.config.mjs tem redirect permanente de /catalog e /catalogo para /. TypeScript compila sem erros.
  </done>
</task>

<task type="auto">
  <name>Task 2: Limpar props orfas do FilterDrawer e atualizar e2e comments</name>
  <files>
    frontend/components/paddle/filter-drawer.tsx,
    frontend/e2e/sliceinsights.spec.ts
  </files>
  <read_first>
    frontend/components/paddle/filter-drawer.tsx,
    frontend/e2e/sliceinsights.spec.ts,
    frontend/components/home-client.tsx
  </read_first>
  <action>
    STEP 1 — Modify `frontend/components/paddle/filter-drawer.tsx`:
    Remove the store-related props that are now dead code (only catalog-client used them):
    - Remove from the props interface (around lines 33-35):
      - `storeFilter?: string;`
      - `onStoreChange?: (slug: string) => void;`
      - `stores?: { name: string; slug: string }[];`
    - Remove from destructuring (around lines 45-46):
      - `storeFilter = "all", onStoreChange,`
      - `stores = []`
    - Remove the `storeActive` count from the active filter calculation (around line 63):
      - `const storeActive = storeFilter !== "all" ? 1 : 0;`
      - Also update the total active count expression to not include `storeActive`
    - Remove the entire Store Filter JSX section (around lines 249-275):
      The block that starts with `{/* Store Filter — only shown when onStoreChange is provided */}`
      and the conditional render `{onStoreChange && stores.length > 0 && (...)}`

    IMPORTANT: Verify that `home-client.tsx` does NOT pass `storeFilter`, `onStoreChange`, or `stores` to FilterDrawer. According to RESEARCH.md, only `catalog-client.tsx` passed those props — the home passes `weightFilter` and `onWeightChange` instead.

    STEP 2 — Modify `frontend/e2e/sliceinsights.spec.ts`:
    - Line 4: Update comment from `Homepage & Catalog` to `Homepage & Paddle Discovery`
    - Line 5: Update comment from `landing → catalog → search → detail` to `landing → search → filter → quiz`
    - Line 37: The `test.describe('Catalog', ...)` block already navigates to `/` and tests home catalog features — rename describe to `'Paddle Discovery'` for clarity
    - DO NOT change line 146 — the text locator `text=/Catálogo|Resultado|Recomendação|catalog|result/i` still works because the home page contains "EXPLORAR CATÁLOGO" and "Catálogo de Raquetes" text
  </action>
  <verify>
    <automated>cd /home/diego/Documentos/projetos/data-products/sliceinsights/frontend && npx tsc --noEmit 2>&1 | tail -5 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "storeFilter" frontend/components/paddle/filter-drawer.tsx` returns 0 — store props removed
    - `grep -c "onStoreChange" frontend/components/paddle/filter-drawer.tsx` returns 0 — store callback removed
    - `grep -c "stores\?" frontend/components/paddle/filter-drawer.tsx` returns 0 — stores prop removed
    - `grep -c "storeActive" frontend/components/paddle/filter-drawer.tsx` returns 0 — store active count removed
    - `grep "describe.*Catalog" frontend/e2e/sliceinsights.spec.ts` returns empty — old describe name gone
    - `grep -c "Paddle Discovery" frontend/e2e/sliceinsights.spec.ts` returns 1 — new describe name present
    - `cd frontend && npx tsc --noEmit` exits with code 0 — TypeScript still compiles
    - `cd frontend && npm run build` exits with code 0 — full build passes
  </acceptance_criteria>
  <done>
    FilterDrawer limpo de props orfas (storeFilter, onStoreChange, stores e logica associada). E2E spec atualizado com nomes que refletem a realidade (Paddle Discovery em vez de Catalog). Build completo passa sem erros.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verificacao final — grep zero-references e build verde</name>
  <files></files>
  <read_first>
    frontend/components/ui/bottom-nav.tsx,
    frontend/app/sitemap.ts,
    frontend/next.config.mjs
  </read_first>
  <action>
    Executar bateria de verificacao final para confirmar que nenhuma referencia ao /catalog sobreviveu no codigo ativo:

    1. `grep -r "from.*@/types/catalog" frontend/ --include="*.tsx" --include="*.ts"` — deve retornar vazio (nenhum import do tipo deletado)
    2. `grep -r "from.*catalog/" frontend/components/ --include="*.tsx" --include="*.ts"` — deve retornar vazio (nenhum import de componente deletado)
    3. `grep -r "href=\"/catalog\"" frontend/ --include="*.tsx" --include="*.ts"` — deve retornar vazio (nenhum link direto)
    4. `grep -r "goto.*catalog" frontend/e2e/ --include="*.ts"` — deve retornar vazio (nenhum teste navega para /catalog)
    5. `cd frontend && npm run build` — deve passar sem erros (build completo verde)

    Se qualquer grep retornar resultado, investigar e corrigir a referencia orfã antes de marcar como concluido.
  </action>
  <verify>
    <automated>cd /home/diego/Documentos/projetos/data-products/sliceinsights && grep -r "from.*@/types/catalog" frontend/ --include="*.tsx" --include="*.ts" | wc -l && grep -r "href=\"/catalog\"" frontend/ --include="*.tsx" --include="*.ts" | wc -l && cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "from.*@/types/catalog" frontend/ --include="*.tsx" --include="*.ts" | wc -l` returns 0
    - `grep -r "from.*catalog/" frontend/components/ --include="*.tsx" --include="*.ts" | wc -l` returns 0
    - `grep -r "href=\"/catalog\"" frontend/ --include="*.tsx" --include="*.ts" | wc -l` returns 0
    - `grep -r "goto.*catalog" frontend/e2e/ --include="*.ts" | wc -l` returns 0
    - `cd frontend && npm run build` exits with code 0
  </acceptance_criteria>
  <done>
    Zero referencias orfas ao /catalog em todo o frontend. Build completo passa. A fase 15.1 esta concluida — codigo morto eliminado, navegacao atualizada, redirect configurado.
  </done>
</task>

</tasks>

<verification>
1. TypeScript check: `cd frontend && npx tsc --noEmit` — passes with zero errors
2. Full build: `cd frontend && npm run build` — passes with zero errors
3. Zero orphan references: `grep -r "catalog" frontend/ --include="*.tsx" --include="*.ts" -l` returns only files where "catalog" appears in user-facing text (e.g., "Catálogo de Raquetes" in home-client.tsx), NOT in import paths or route references
4. Redirect works: `grep "source.*catalog" frontend/next.config.mjs` confirms redirect rule exists
5. Nav updated: `grep "recommend" frontend/components/ui/bottom-nav.tsx` confirms new nav link
</verification>

<success_criteria>
- Diretorio `frontend/app/catalog/` nao existe
- Diretorio `frontend/components/catalog/` nao existe
- Arquivo `frontend/types/catalog.ts` nao existe
- Bottom-nav tem NavItem para /recommend com icone Sparkles e label "IA"
- Sitemap tem /recommend e nao tem /catalogo
- next.config.mjs tem redirects para /catalog e /catalogo -> /
- FilterDrawer nao tem props storeFilter/onStoreChange/stores
- E2E spec renomeou describe de 'Catalog' para 'Paddle Discovery'
- `npm run build` passa sem erros
- Zero imports orfaos de @/types/catalog ou componentes catalog/
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-remover-pagina-de-catalogo-pois-ja-existe-na-home-com-melhores-filtros/15.1-01-SUMMARY.md`
</output>
