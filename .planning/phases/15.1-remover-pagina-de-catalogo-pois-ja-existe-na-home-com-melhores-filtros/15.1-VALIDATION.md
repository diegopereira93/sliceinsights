---
phase: 15.1
slug: remover-pagina-de-catalogo-pois-ja-existe-na-home-com-melhores-filtros
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-22
---

# Phase 15.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Next.js build + pytest + Playwright E2E |
| **Config file** | `frontend/next.config.mjs` / `pytest.ini` / `e2e/sliceinsights.spec.ts` |
| **Quick run command** | `cd frontend && npm run build 2>&1 | tail -5` |
| **Full suite command** | `cd frontend && npm run build && cd .. && pytest tests/ -q` |
| **Estimated runtime** | ~60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd frontend && npm run build 2>&1 | tail -5`
- **After every plan wave:** Run `cd frontend && npm run build && cd .. && pytest tests/ -q`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 15.1-01-01 | 01 | 1 | cleanup | build | `cd frontend && npm run build 2>&1 \| grep -E "error\|✓"` | ✅ | ⬜ pending |
| 15.1-01-02 | 01 | 1 | cleanup | build | `cd frontend && npm run build 2>&1 \| grep -E "error\|✓"` | ✅ | ⬜ pending |
| 15.1-01-03 | 01 | 1 | cleanup | e2e | `cd frontend && npx playwright test e2e/ --reporter=line 2>&1 \| tail -10` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `/catalog` retorna 404 ou redireciona para `/` | cleanup | Requires browser navigation | Open `/catalog` in browser — expect redirect or 404, NOT the old catalog page |
| Bottom-nav não tem mais link "Catálogo" | cleanup | Visual check | Open app on mobile viewport — confirm no "Catálogo" NavItem in bottom nav |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
