---
phase: 15.2-fix-statistics-page-content-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [STATS-01]

must_haves:
  truths:
    - "Frontend Docker container has a fresh .next build with correct chunk hashes"
    - "Navigating to /statistics does not produce 404 errors for _next/static/chunks/"
    - "Client-side hydration completes — Overview tab shows actual content"
  artifacts:
    - path: "Docker container picklematch_ui_v3"
      provides: "Running frontend with fresh build"
  key_links:
    - from: "Docker frontend_next service"
      to: ".next/static/chunks/"
      via: "next build inside container"
      pattern: "docker compose build frontend_next"
---

<objective>
Rebuild the Docker frontend container to produce a fresh `.next` build, eliminating stale chunk 404 errors that break client-side hydration on /statistics.

Purpose: The `.next` directory contains root-owned files from a prior Docker build. The dev server generates new JS chunk IDs but the old files are locked (permission denied), causing `main-app.js`, `layout.js`, and other core chunks to return 404. This breaks client hydration so the Overview tab renders blank.

Output: Running `picklematch_ui_v3` container with a fresh `.next` build — zero 404 errors for `_next/static/chunks/` resources.
</objective>

<execution_context>
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/workflows/execute-plan.md
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-fix-statistics-page-content-rendering/15.2-CONTEXT.md
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Docker frontend container with fresh .next build</name>
  <files></files>
  <read_first>
    - docker-compose.yml (confirm service name is `frontend_next`, container name `picklematch_ui_v3`)
    - .planning/phases/15.2-fix-statistics-page-content-rendering/15.2-CONTEXT.md (root cause details)
  </read_first>
  <action>
1. Run from project root: `docker compose build --no-cache frontend_next` to force a full rebuild of the frontend service, producing a fresh `.next` directory with correct chunk hashes.
2. After build completes, bring the container up: `docker compose up -d frontend_next`
3. Wait 10-15 seconds for the Next.js dev server to compile and be ready.
4. Verify the container is running: `docker ps --filter name=picklematch_ui_v3 --format "table {{.Names}}\t{{.Status}}"` — status should show "Up".
5. Quick-check that the dev server is responding: `curl -s -o /dev/null -w "%{http_code}" http://localhost:3002/statistics` — expect 200.

IMPORTANT: The docker-compose service name is `frontend_next` (NOT `frontend`). The container name is `picklematch_ui_v3`.
  </action>
  <verify>
    <automated>docker ps --filter name=picklematch_ui_v3 --format "{{.Status}}" | grep -q "Up" && curl -s -o /dev/null -w "%{http_code}" http://localhost:3002/statistics | grep -q "200" && echo "PASS: container up and /statistics returns 200" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `docker ps` shows `picklematch_ui_v3` with status "Up"
    - `curl http://localhost:3002/statistics` returns HTTP 200
    - No permission-denied errors in container logs for `.next/static/chunks/`
  </acceptance_criteria>
  <done>Docker frontend container rebuilt and running with fresh .next build; /statistics returns 200</done>
</task>

</tasks>

<verification>
- Container `picklematch_ui_v3` is running (docker ps)
- HTTP 200 from http://localhost:3002/statistics
- Container logs show no `.next` permission errors: `docker logs picklematch_ui_v3 --tail 50 2>&1 | grep -i "permission\|EACCES"` returns empty
</verification>

<success_criteria>
Frontend Docker container is running with a fresh .next build. The /statistics page returns HTTP 200 and the dev server is ready to serve JS chunks without 404 errors.
</success_criteria>

<output>
After completion, create `.planning/phases/15.2-fix-statistics-page-content-rendering/15.2-01-SUMMARY.md`
</output>
