---
phase: 15.2-fix-statistics-page-content-rendering
plan: 03
type: execute
wave: 2
depends_on: [15.2-01, 15.2-02]
files_modified: []
autonomous: false
requirements: [STATS-03]

must_haves:
  truths:
    - "/statistics Overview tab shows paddle cards and leaderboards (not blank)"
    - "/statistics Comparativos tab shows scatter charts"
    - "Zero console 404 errors for _next/static/chunks/"
    - "/recommend page still works correctly"
  artifacts: []
  key_links:
    - from: "Browser client"
      to: "Next.js dev server"
      via: "_next/static/chunks/ JS bundles"
      pattern: "_next/static/chunks"
    - from: "/statistics Overview tab"
      to: "API /api/v1/catalog/paddles"
      via: "fetch for paddle data"
      pattern: "catalog/paddles"
---

<objective>
End-to-end verification of the /statistics page and /recommend page using playwright-cli after Docker rebuild and code commit.

Purpose: Confirm that the Docker rebuild (Plan 01) and code fixes (Plan 02) together resolve the blank statistics page. Verify no regressions on /recommend.

Output: Verified working /statistics page (all tabs) and /recommend page. No 404 console errors.
</objective>

<execution_context>
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/workflows/execute-plan.md
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-fix-statistics-page-content-rendering/15.2-CONTEXT.md
@.planning/phases/15.2-fix-statistics-page-content-rendering/15.2-01-SUMMARY.md
@.planning/phases/15.2-fix-statistics-page-content-rendering/15.2-02-SUMMARY.md
@.claude/skills/playwright-cli/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E verification of /statistics and /recommend via playwright-cli</name>
  <files></files>
  <read_first>
    - .claude/skills/playwright-cli/SKILL.md (playwright-cli command reference)
    - .planning/phases/15.2-fix-statistics-page-content-rendering/15.2-CONTEXT.md (expected behaviors)
    - .planning/phases/15.2-fix-statistics-page-content-rendering/15.2-01-SUMMARY.md (Docker rebuild result)
    - .planning/phases/15.2-fix-statistics-page-content-rendering/15.2-02-SUMMARY.md (commit result)
  </read_first>
  <action>
Perform E2E verification using playwright-cli. The frontend runs at http://localhost:3002.

**Step 1 — Check for 404 console errors:**
1. `playwright-cli open http://localhost:3002/statistics`
2. `playwright-cli console error` — capture any console errors. There should be ZERO 404 errors for `_next/static/chunks/`.
3. Take a snapshot: `playwright-cli snapshot`

**Step 2 — Verify Overview tab content:**
1. The /statistics page loads on the Overview tab by default.
2. `playwright-cli snapshot` — verify the snapshot shows paddle cards (not blank content). Look for elements with paddle names, leaderboard entries, or card components. The header "RAIO-X DO MERCADO" should be visible along with actual data content below the tabs.

**Step 3 — Verify Comparativos tab:**
1. Find and click the "Comparativos" tab element in the snapshot.
2. `playwright-cli snapshot` — verify scatter chart elements are rendered (look for SVG elements, chart containers, or recharts components). The tab content should NOT be blank.

**Step 4 — Verify /recommend still works:**
1. `playwright-cli goto http://localhost:3002/recommend`
2. `playwright-cli snapshot` — verify the recommendation wizard or page content loads correctly. Should show the player profile quiz or recommendation interface.

**Step 5 — Verify / redirect from /catalog:**
1. `playwright-cli goto http://localhost:3002/catalog`
2. Confirm it redirects to `/` (check page URL in snapshot output).

**Step 6 — Close browser:**
1. `playwright-cli close`

If ANY verification step fails, document exactly what was observed vs expected.
  </action>
  <verify>
    <automated>playwright-cli open http://localhost:3002/statistics && playwright-cli console error 2>&1 | grep -c "_next/static/chunks" | grep -q "^0$" && echo "PASS: no chunk 404s" || echo "FAIL: chunk 404s detected"</automated>
  </verify>
  <acceptance_criteria>
    - `playwright-cli console error` output contains ZERO lines mentioning `_next/static/chunks`
    - /statistics Overview tab snapshot shows paddle content (card elements, leaderboard text — NOT blank below tabs)
    - /statistics Comparativos tab snapshot shows chart elements (SVG or recharts containers)
    - /recommend page snapshot shows wizard/recommendation interface content
    - /catalog redirects to / (URL in snapshot shows localhost:3002/)
  </acceptance_criteria>
  <done>All /statistics tabs render content, zero 404 chunk errors, /recommend works, /catalog redirects to /</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of statistics page rendering</name>
  <files></files>
  <action>
Present verification instructions to the user. All automation was completed in Task 1.
The user must visually confirm that the statistics page renders correctly across all 4 tabs.

What was built:
- Docker frontend rebuilt with fresh .next build (Plan 01)
- 3 frontend fixes committed (Plan 02)
- Automated E2E verification passed (Task 1 above)

How to verify:
1. Open http://localhost:3002/statistics in your browser
2. Overview tab: Verify you see paddle cards and leaderboards with actual data (not blank)
3. Comparativos tab: Click "Comparativos" — verify scatter charts appear
4. Rankings tab: Click "Rankings" — verify ranking tables appear
5. Marcas tab: Click "Marcas" — verify brand intelligence data appears
6. Open browser DevTools (F12) > Network tab > reload page — confirm no 404 errors for JS chunks
7. Open http://localhost:3002/recommend — verify the recommendation page still works

Resume signal: Type "approved" if all tabs render correctly, or describe any issues you see.
  </action>
  <verify>User confirms "approved"</verify>
  <acceptance_criteria>
    - User confirms all 4 statistics tabs render content
    - User confirms no 404 errors in DevTools Network tab
    - User confirms /recommend page is unaffected
  </acceptance_criteria>
  <done>User has visually verified all statistics tabs render correctly and /recommend is unaffected</done>
</task>

</tasks>

<verification>
- Automated E2E via playwright-cli confirms all tabs have content
- Human visual verification confirms correct rendering
- Zero console 404 errors for _next/static/chunks/
- /recommend page unaffected by changes
</verification>

<success_criteria>
All 4 statistics tabs (Overview, Comparativos, Rankings, Marcas) render their content correctly. No 404 errors for JS chunks. /recommend page is unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/15.2-fix-statistics-page-content-rendering/15.2-03-SUMMARY.md`
</output>
