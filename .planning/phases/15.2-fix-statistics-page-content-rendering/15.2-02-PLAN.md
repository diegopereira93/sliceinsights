---
phase: 15.2-fix-statistics-page-content-rendering
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/statistics-client.tsx
  - frontend/components/statistics/brand-intelligence.tsx
  - frontend/tsconfig.json
autonomous: true
requirements: [STATS-02]

must_haves:
  truths:
    - "All 3 pending frontend changes are committed to git"
    - "statistics-client.tsx uses getTop('power', ...) instead of getTop('powerOriginal', ...)"
    - "statistics-client.tsx conditionally renders Comparativos charts only when activeTab === 'comparativos'"
    - "brand-intelligence.tsx filters brands with count >= 1 (not >= 2)"
    - "tsconfig.json includes .next-clean/types/**/*.ts"
  artifacts:
    - path: "frontend/components/statistics-client.tsx"
      provides: "Corrected power field + conditional chart rendering"
    - path: "frontend/components/statistics/brand-intelligence.tsx"
      provides: "Lowered brand filter threshold"
    - path: "frontend/tsconfig.json"
      provides: "Extended include paths"
  key_links:
    - from: "statistics-client.tsx"
      to: "getTop helper"
      via: "field name 'power' (not 'powerOriginal')"
      pattern: "getTop\\('power'"
    - from: "brand-intelligence.tsx"
      to: "brand filter"
      via: "count >= 1 threshold"
      pattern: "count >= 1"
---

<objective>
Commit the 3 pending frontend changes that are already in the working tree: field name fix, conditional chart rendering, brand filter threshold, and tsconfig include path.

Purpose: These changes are valid improvements sitting uncommitted. They fix a leaderboard field bug, add a performance optimization for tab rendering, lower the brand filter threshold for small datasets, and extend the tsconfig include paths.

Output: Clean git commit with all 3 files staged and committed.
</objective>

<execution_context>
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/workflows/execute-plan.md
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-fix-statistics-page-content-rendering/15.2-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and commit pending frontend changes</name>
  <files>
    frontend/components/statistics-client.tsx
    frontend/components/statistics/brand-intelligence.tsx
    frontend/tsconfig.json
  </files>
  <read_first>
    - .planning/phases/15.2-fix-statistics-page-content-rendering/15.2-CONTEXT.md (exact changes expected)
    - frontend/components/statistics-client.tsx (verify getTop('power', ...) and activeTab conditions present)
    - frontend/components/statistics/brand-intelligence.tsx (verify count >= 1)
    - frontend/tsconfig.json (verify .next-clean/types/**/*.ts in include)
  </read_first>
  <action>
1. Verify the 3 files have the expected changes by running `git diff` on each:
   - `git diff frontend/components/statistics-client.tsx` — confirm `getTop('power', ...)` (not `powerOriginal`) and `activeTab === 'comparativos' &&` conditions on chart renders
   - `git diff frontend/components/statistics/brand-intelligence.tsx` — confirm `count >= 1` (not `>= 2`)
   - `git diff frontend/tsconfig.json` — confirm `.next-clean/types/**/*.ts` added to include array

2. Stage ONLY these 3 files (do NOT use `git add .` or `git add -A`):
   ```bash
   git add frontend/components/statistics-client.tsx frontend/components/statistics/brand-intelligence.tsx frontend/tsconfig.json
   ```

3. Commit with descriptive message:
   ```
   fix(statistics): correct power field, conditional chart render, brand filter threshold

   - statistics-client.tsx: getTop('power',...) replaces getTop('powerOriginal',...)
   - statistics-client.tsx: charts render only when activeTab === 'comparativos'
   - brand-intelligence.tsx: filter count >= 1 (was >= 2) for small datasets
   - tsconfig.json: add .next-clean/types/**/*.ts to include

   Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
   ```

4. Verify commit succeeded: `git log --oneline -1` and `git status` should show clean working tree for those 3 files.

IMPORTANT: Do NOT commit .planning/ROADMAP.md or .planning/STATE.md in this task — those are planning files managed separately.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -q "fix(statistics)" && git diff --name-only frontend/components/statistics-client.tsx frontend/components/statistics/brand-intelligence.tsx frontend/tsconfig.json | wc -l | grep -q "^0$" && echo "PASS: commit exists and files are clean" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` shows commit message starting with "fix(statistics)"
    - `git diff frontend/components/statistics-client.tsx` is empty (no uncommitted changes)
    - `git diff frontend/components/statistics/brand-intelligence.tsx` is empty
    - `git diff frontend/tsconfig.json` is empty
    - `grep -n "getTop('power'" frontend/components/statistics-client.tsx` finds at least one match
    - `grep -n "count >= 1" frontend/components/statistics/brand-intelligence.tsx` finds a match
    - `grep -n "next-clean" frontend/tsconfig.json` finds a match
  </acceptance_criteria>
  <done>All 3 pending frontend files committed with descriptive message; git status shows no uncommitted changes for those files</done>
</task>

</tasks>

<verification>
- `git log --oneline -1` shows the fix(statistics) commit
- `git status` shows the 3 frontend files are no longer in "Changes not staged"
- The commit contains exactly 3 files: `git diff-tree --no-commit-id --name-only -r HEAD` lists all 3
</verification>

<success_criteria>
All 3 pending frontend changes are committed to git in a single descriptive commit. No other files are accidentally included.
</success_criteria>

<output>
After completion, create `.planning/phases/15.2-fix-statistics-page-content-rendering/15.2-02-SUMMARY.md`
</output>
