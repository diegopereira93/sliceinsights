---
phase: 14-web-catalog-page
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified: []
autonomous: false
requirements: [WEB-01, WEB-02, WEB-03]

must_haves:
  truths:
    - "User confirms catalog page loads with paddle images, brand badges, prices, and store links"
    - "User confirms filters work and URL updates"
    - "User confirms bottom nav Catalogo link works"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete catalog page — visual check, filter interaction, URL sharing, and store link behavior.

Purpose: Confirm the catalog page meets all WEB-01, WEB-02, WEB-03 requirements before marking the phase complete.
Output: User approval or list of issues to fix.
</objective>

<execution_context>
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/workflows/execute-plan.md
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-web-catalog-page/14-CONTEXT.md
@.planning/phases/14-web-catalog-page/14-UI-SPEC.md
@.planning/phases/14-web-catalog-page/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the project and verify /catalog route compiles</name>
  <read_first>
    - frontend/package.json
  </read_first>
  <files>frontend/app/catalog/page.tsx</files>
  <action>
Run the Next.js build to verify all catalog-related files compile without errors:

1. Run `cd frontend && npx next build` to produce a production build.
2. Check output for errors in catalog-related files (`app/catalog/`, `components/catalog/`, `types/catalog.ts`).
3. If build errors occur in catalog files, fix them (likely TypeScript type mismatches or missing imports).
4. Pre-existing warnings in non-catalog files are acceptable — do not fix unrelated issues.
5. Confirm the build output lists the `/catalog` route as compiled.

After successful build, start the dev server with `cd frontend && npm run dev` in the background so the user can test manually.
  </action>
  <verify>
    <automated>cd /home/diego/Documentos/projetos/data-products/sliceinsights/frontend && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx next build` exit code is 0
    - Build output contains `/catalog` in the route list
    - No errors referencing `app/catalog/`, `components/catalog/`, or `types/catalog.ts`
  </acceptance_criteria>
  <done>Next.js production build succeeds with /catalog route compiled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of catalog page</name>
  <files>frontend/app/catalog/page.tsx</files>
  <action>
Present the running catalog page to the user for manual verification. The dev server should be running at http://localhost:3000. Walk the user through the 12-step verification checklist covering WEB-01 (page renders paddles), WEB-02 (filter controls), and WEB-03 (dynamic updates). Wait for user approval or issue list.
  </action>
  <verify>
    <automated>echo "Human verification — awaiting user response"</automated>
  </verify>
  <what-built>
    Complete web catalog page at /catalog with:
    - Paddle listing grid (3/2/1 columns responsive)
    - Glass-card design with paddle image, brand/thickness/material badges, price, "Ver na [StoreName]" button
    - Filter drawer with: thickness (14mm/16mm), surface material (Carbon/Fiberglass), price range slider, brand search, store selection
    - 400ms debounce on filter changes
    - URL reflects active filters (shareable links)
    - Pagination (24 per page)
    - Skeleton loading cards during fetch
    - Empty state when no results match
    - "Catalogo" in bottom nav
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3000/catalog — verify paddle cards appear with images, brand badges, and prices
    2. Click "Filtros" button — verify the drawer opens with all 5 filter sections (Espessura, Material da Face, Preco, Marcas, Loja)
    3. Select a thickness filter (e.g. 16mm) — verify:
       a. URL updates to include ?core_thickness=16
       b. Paddle grid updates to show only 16mm paddles
       c. A filter chip appears in the bar showing "16mm" with X to dismiss
    4. Select a brand — verify URL adds &brand=... and results filter
    5. Copy the URL with filters, open in a new tab — verify filters are pre-applied (shareable link)
    6. Click "Ver na [StoreName]" on any card — verify it opens the store page in a new tab
    7. Apply filters that match no paddles — verify empty state shows "Nenhuma raquete encontrada" with "Limpar filtros" button
    8. Click "Limpar filtros" — verify all filters reset and full listing returns
    9. Scroll to bottom — verify pagination "Anterior"/"Proxima" buttons work
    10. Check bottom nav — verify "Catalogo" link with ShoppingBag icon is present and active when on /catalog
    11. Navigate to / (Home) — verify the home page FilterDrawer still works correctly (no regression)
    12. Resize browser to mobile width (375px) — verify grid goes to 1 column and filter drawer is a bottom sheet
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe specific issues to fix</resume-signal>
  <done>User has verified catalog page meets WEB-01, WEB-02, WEB-03 requirements and approved or provided issues.</done>
</task>

</tasks>

<verification>
Human verification covers all three requirements:
- WEB-01: Catalog page renders with paddle listing (step 1)
- WEB-02: Filter controls work (steps 2-4, 7-8)
- WEB-03: Dynamic filter updates (steps 3-5)
</verification>

<success_criteria>
- User confirms "approved" for the catalog page
- Or: User provides specific issues list, which triggers gap closure
</success_criteria>

<output>
After completion, create `.planning/phases/14-web-catalog-page/14-03-SUMMARY.md`
</output>
