---
phase: 14-web-catalog-page
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified: []
autonomous: false
requirements: [WEB-01, WEB-02, WEB-03]

must_haves:
  truths:
    - "User confirms home page loads with paddle cards showing images, brand badges, and prices"
    - "User confirms search bar and filter drawer work correctly"
    - "User confirms bottom nav IA link works"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the home page catalog functionality — visual check of paddle cards, search interaction, filter behavior, and navigation.

Note: The /catalog route was removed in Phase 15.1 and replaced by the home page (/) which now contains all catalog functionality. This plan verifies the equivalent functionality on the home page.

Purpose: Confirm the home page catalog meets all WEB-01, WEB-02, WEB-03 requirements before marking the phase complete.
Output: User approval or list of issues to fix.
</objective>

<execution_context>
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/workflows/execute-plan.md
@/home/diego/Documentos/projetos/data-products/sliceinsights/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-web-catalog-page/14-CONTEXT.md
@.planning/phases/14-web-catalog-page/14-UI-SPEC.md
@.planning/phases/14-web-catalog-page/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification</name>
  <action>
The build was already completed successfully in this session. The Next.js production build succeeded with:
- Compiled successfully
- Type checking passed
- No errors in catalog-related files
- Static pages generated (9/9)

The dev server should already be running. If not, start it with `cd frontend && npm run dev`.
  </action>
  <verify>
    <automated>echo "Build verified: npx next build completed successfully"</automated>
  </verify>
  <acceptance_criteria>
    - Next.js build succeeded (verified in this session)
    - Dev server is accessible at localhost:3000
  </acceptance_criteria>
  <done>Build verified, dev server running at localhost:3000</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of home page catalog</name>
  <action>
Present the running home page to the user for manual verification. The dev server should be running at http://localhost:3000. Walk the user through the verification checklist covering WEB-01 (paddle cards), WEB-02 (filter controls), and WEB-03 (search and filter interaction).
  </action>
  <verify>
    <automated>echo "Human verification — awaiting user response"</automated>
  </verify>
  <what-built>
    Home page (/) catalog functionality with:
    - Hero section with "ENCONTRAR MINHA RAQUETE" quiz and "EXPLORAR CATÁLOGO" button
    - Sticky search bar with real-time filtering
    - FilterDrawer with: brands (multi-select with search), price range, weight filter, thickness filter
    - Paddle grid (4/2/1 columns responsive) with paddle cards
    - Empty state with "Limpar Todos os Filtros" button
    - Bottom nav with "IA" link to /recommend (Sparkles icon)
    - PaddleDetailDrawer for detailed view
    - PaddleComparator for battle mode
    - RacketFinderQuiz AI assistant
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3000 — verify hero section appears with "ENCONTRAR MINHA RAQUETE" and "EXPLORAR CATÁLOGO" buttons
    2. Scroll down to the sticky toolbar — verify search bar and "Filtros" button are visible
    3. Verify paddle cards appear with images, brand badges, prices, and compare button
    4. Click "Filtros" button — verify the drawer opens with filter sections (Espessura, Preco, Marcas, Peso)
    5. Select a thickness filter (e.g. 16mm) — verify paddle grid updates to show only matching paddles
    6. Type in the search bar — verify real-time filtering by paddle name or brand
    7. Apply filters that match no paddles — verify empty state shows "Nenhuma raquete encontrada" with "Limpar Todos os Filtros" button
    8. Click "Limpar Todos os Filtros" — verify all filters reset and full listing returns
    9. Click on a paddle card — verify PaddleDetailDrawer opens with full details
    10. Click "Comparar" on two different cards — verify "Modo de Batalha" bar appears
    11. Click "LUTAR!" — verify PaddleComparator modal opens with side-by-side comparison
    12. Check bottom nav — verify "IA" link with Sparkles icon routes to /recommend
    13. Click "ENCONTRAR MINHA RAQUETE" — verify RacketFinderQuiz modal opens
    14. Resize browser to mobile width (375px) — verify grid goes to 1 column and filter drawer is a bottom sheet
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe specific issues to fix</resume-signal>
  <done>User has verified home page catalog meets WEB-01, WEB-02, WEB-03 requirements and approved or provided issues.</done>
</task>

</tasks>

<verification>
Human verification covers all three requirements:
- WEB-01: Home page renders with paddle listing (steps 3, 6)
- WEB-02: Filter controls work (steps 4, 5, 7, 8)
- WEB-03: Search and filter interaction (steps 6, 7)
</verification>

<success_criteria>
- User confirms "approved" for the home page catalog
- Or: User provides specific issues list, which triggers gap closure
</success_criteria>

<output>
After completion, create `.planning/phases/14-web-catalog-page/14-03-SUMMARY.md`
</output>
